# Task: 接续 Kimi CLI agent 完成本地订阅识别扩展

## 原始 prompt（用户给 kimi CLI 的）

1. 把本地 worktree 里的菜单栏图标 bar 功能改动合并到 main
2. 检查并清空所有 worktree
3. 列出当前的 requirements

> 下一步最重要的工作是确保真的能获取到本地的订阅。现在只有 Codex 能正确获取到。我的电脑上至少有 Trae Work、Kimi、MiniMax 和 Google 的 Antigravity。其中除了 Trae Work 以外，所有的都有 CLI，Quota Bar 应该尝试从 app 和 cli 获取订阅，确保即使只安装其中一个也能正确识别到订阅信息；并且能识别两个工具用的是否是一个订阅权限/用户，确保如果用的是两个权限，能正确地拆分成两个权限的信息；如果两个用的是同一个权限，能正确合并成一个权限的信息。

## 启动状态

- 分支：`main` @ `926bd65`，up to date with origin/main
- worktree：之前 kimi CLI agent 已经全部清空，只剩 main
- 未提交改动：上一轮（kimi CLI agent 完成 Codex OAuth + 价格本地化 等 P1 内容）大量文件 modified，未 commit；新增 `CodexAuthProvider.swift` 和 `research/codex-bar-comparison.md`
- REQUIREMENTS.md：上一轮已经把 SUB-A-009/010/011 等加进去

## 接手做的工作

### 新增 / 改动文件

- `QuotaModels.swift`：加 `.trae` / `.antigravity` 枚举 + `bundleIdentifier`（用 `defaults read` 拿到的真实 ID：`com.trae.solo.app`、`com.google.antigravity`、`com.moonshot.kimichat`、`com.minimax.agent.cn`）+ `cliCommand` (Kimi/MiniMax) + brand color/icon
- `KimiAuthProvider.swift`：新文件。从 `~/.kimi-code/credentials/kimi-code.json` 读 OAuth token → POST `apiv2/kimi.gateway.billing.v1.BillingService/GetUsages`；401 → `missingCredentials` 让 pipeline fallback。JWT 解码 `device_id`/`ssid`/`sub` 加 session headers。
- `AppBundleProvider.swift`：新文件。通用 App 探测：先查 `NSWorkspace` bundleID，再 fallback 到候选 `App.app` 路径。命中 → `needsConfiguration("App 已安装，dashboard 暂未接入")`，未命中 → throw `sourceUnavailable`。
- `Strategies.swift`：给 Kimi pipeline 加 `[KimiAuthProvider, BrowserCookie, Keychain, AppBundle]`；给 MiniMax pipeline 加 `AppBundle` 兜底；新增 traePipeline / antigravityPipeline（只有 AppBundle + Keychain）。
- `RefreshCoordinator.swift`：filter 改为保留 `.available` + `.needsConfiguration`，过滤 `.notInstalled` + `.fetchFailed`。这样 Trae / Antigravity / 没登录的 Kimi 都会以「待配置」展示。
- `AgentDetector.swift`：`findApp` 通用化，加 Trae / Antigravity / Kimi / MiniMax 的 App 名 fallback。
- `REQUIREMENTS.md`：加 SUB-A-012/013 完成项，SUB-A-014~017 deferred（Web cookie parser / 多身份合并）。

### 本机实际探测结果（先验证再设计）

- `~/.kimi-code/credentials/kimi-code.json` 里有 OAuth access_token（JWT，scope=kimi-code），但 `POST /apiv2/.../GetUsages` 401 `REASON_INVALID_AUTH_TOKEN` —— CLI token 不能直接调 web endpoint
- `~/.mavis/config.yaml` 里的 `apiKey: sk-xxx` 是脱敏过的（用户隐藏真 key），不是真 key
- MiniMax web endpoint `api.minimax.chat/v1/api/openplatform/coding_plan/remains` 在浏览器签名检查下被 Cloudflare 1010 拦截；带 cookie 走能命中但要 web cookie
- **结论**：CLI OAuth token 路径在本机不可直接用于拿 dashboard 数据，Kimi/MiniMax 真实可行的数据源只有浏览器 Cookie（用户在浏览器登录 kimi.com / minimax.chat）

### 运行验证日志（FileHandle 输出后已删）

```
QB: codex ok av=available q=2 tier=Plus
QB: kimi ok av=needsConfiguration(reason: "App 已安装，dashboard 暂未接入") q=0 tier=-
QB: minimax ok av=needsConfiguration(reason: "App 已安装，dashboard 暂未接入") q=0 tier=-
QB: trae ok av=needsConfiguration(reason: "App 已安装，dashboard 暂未接入") q=0 tier=-
QB: antigravity ok av=needsConfiguration(reason: "App 已安装，dashboard 暂未接入") q=0 tier=-
QB: gemini ok av=notInstalled q=0 tier=-
QB: claude ok av=notInstalled q=0 tier=-
```

7 个 provider 全部被识别。dashboard 里展示 Codex（真实 Plus 额度）+ Kimi/MiniMax/Trae/Antigravity（已安装但需在浏览器登录），过滤 Gemini/Claude（未登录且无 App）。

### 关于「多身份合并/拆分」

用户提到"两个工具用的是否是一个订阅权限"。当前架构每个 `ProviderKind` 一条 snapshot，**没有实现**多身份识别。已列为 SUB-A-017 #deferred（#P2）。本轮专注"识别到服务"，身份合并拆分的全链路改造留到下一轮。

关于数据源身份（API key 前 8 位、OAuth sub、JWT `sub`）：sub-bro 的 `MiniMaxAPIRegion` / `MiniMaxLocalStorageImporter` 已有部分基础设施；后续要做时先在 `ProviderSnapshot` 加 `subscriptionIdentity: String?`，pipeline 多返回 `[ProviderSnapshot]`，RefreshCoordinator 按 `(kind, identity)` 分组。

## 已知遗留问题

1. **Edge Safe Storage 弹窗**：Kimi/MiniMax/Claude/Gemini pipeline 走 BrowserCookieProvider 时会触发（SweetCookieKit 行为，跟 CodexBar 一样）。需要 CodexBar 的 `BrowserCookieAccessGate` 类似机制在 `coordinator.refreshCycle` 一次性提示用户。
2. **截图验证没成功**：AppleScript 没点中 status bar item 的菜单，但日志已证 dashboard 数据正确。
3. **Kimi CLI token 不直接可用**：401 时 KimiAuthProvider 转 missingCredentials 让 pipeline fallback 到 Cookie/Keychain/App 兜底，是合理的。

## Build / Run

- `swift build`：通过
- `swift run`：通过，启动后 ~5s 内 dashboard 数据就绪

## 分支

没改分支，全部改动在 `main` 上，未提交。
