# 用户原始 prompt

> 打不开？

# 启动运行时的分支和版本

- 分支：`preferences/main`
- 提交：`358a2fb`
- 备注：延续同一偏好设置 UI 调整任务，当前工作区已有未提交改动；本轮未执行 `git pull`。

# 任务开始时间

- 2026-06-30 23:44:00 +0800

# 任务结束时间

- 2026-06-30 23:47:10 +0800

# 任务结束时是否执行了提交

- 否。

# 已阅读上下文

- `quota-bar/scripts/build-app.sh`
- `quota-bar/Sources/QuotaBar/Preferences/AboutSettingsView.swift`
- `quota-bar/build/latest/QuotaBar.app/Contents/Info.plist`

# 对话与行动记录

- 用户反馈最新 `.app` 打不开。
- 检查最新包的 `Info.plist`，发现 `CFBundleVersion` 被写成 `260630.234111.preferences-main`，包含字母和连字符，不适合作为 macOS 系统 bundle build 字段。
- 同时发现 `CFBundleIdentifier` 仍为 `com.example.QuotaBar`，与签名 identifier `com.taobe.quotabar` 不一致。
- 修改打包脚本：
  - `CFBundleVersion` 改为纯数字时间格式 `yymmdd.hhmmss`。
  - 新增 `QBDisplayBuild` 保存用户可见格式 `yymmdd.hhmmss.<branchname>`。
  - `CFBundleIdentifier` 改为 `com.taobe.quotabar`。
- 修改关于页 build 文案读取逻辑，优先读取 `QBDisplayBuild`，否则 fallback 到 `CFBundleVersion`。
- 重新构建、测试、打包并验证 plist 与签名。

# 完成工作

- 修复最新 `.app` 可能因非法 `CFBundleVersion` 打不开的问题。
- 保留关于页显示带分支名 build 号的需求。
- 修正 bundle identifier 与签名 identifier 不一致。

# 更新的需求 ID

- 延续偏好设置关于页 build 展示需求；本轮未新增独立需求 ID。

# 更新的 README 或 DESIGN 章节

- 无。

# 验证方式

- `cd quota-bar && swift build`：通过。
- `cd quota-bar && swift test`：通过，20 个测试全部通过。
- `make app`：通过，产物为 `quota-bar/build/20260630-234643-preferences-main/QuotaBar.app`。
- `PlistBuddy Print :CFBundleVersion`：输出 `260630.234643`。
- `PlistBuddy Print :QBDisplayBuild`：输出 `260630.234643.preferences-main`。
- `PlistBuddy Print :CFBundleIdentifier`：输出 `com.taobe.quotabar`。
- `codesign --verify --deep --strict --verbose=2 quota-bar/build/latest/QuotaBar.app`：通过。

# 备注

- macOS 系统 bundle 字段保持严格格式；用户可见 build 元信息放自定义 plist key，避免 LaunchServices 拒绝或异常处理应用包。
