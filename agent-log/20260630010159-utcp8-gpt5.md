# 2026-06-30 01:01 — Preferences 视觉微调（标题、说明、列表密度）

## 用户原始 prompt

> 为我做两个调整：
>
> 1. 所有选项的小字描述需要像 macOS 系统菜单一样，用横线分割之后再写上，参考附件2
> 2. 每一个页面需要有标题，标题可以参考 Veo Island 的设置选单，直接复刻左边的标题就行，注意 padding 就可以，参考附件一
> 3. 所有的（比如 provider 这边的）开关都有些太大了，请调整一下比例，参考附件 2。
> 4. 并且这种多个对象的页面，我觉得你的 padding 也有点问题，同样参考附件 2。
>
> 最后就是移除收起菜单左侧边栏的按钮

## 启动运行时的分支和版本

- 分支：`preferences/main`
- HEAD：`358a2fb` refactor(preferences): 背景色从 .regularMaterial 玻璃改极浅灰（对齐软件更新页）
- 上游：`origin/feat/preferences`，本地 ahead 6
- `git pull`：Already up to date.

## 任务开始时间

- 2026-06-30 00:49 (Asia/Shanghai, UTC+8)

## 任务结束时间

- 2026-06-30 01:01 (Asia/Shanghai, UTC+8)

## 任务结束时是否执行了提交

- 否

## 已阅读上下文

- `REQUIREMENTS.md` 中 v0.3.0 PM-A 偏好设置需求区块
- `SettingsComponents.swift`
- `PreferencesScene.swift`
- `GeneralSettingsView.swift`
- `ModelsSettingsView.swift`
- `ActivationSettingsView.swift`
- `AboutSettingsView.swift`
- `PreferencesWindowController.swift`
- 用户提供的两张参考截图

## 对话与行动记录

- 新增需求 `[0.3.0-PM-A-012]`，记录本次 Preferences 视觉微调范围。
- 新增共享 `SettingsPage` / `SettingsPageTitle`，每个 detail 页面顶部显示与 sidebar 一致的图标和标题。
- 调整 `SettingsRow`：说明文字前先画行内分隔线，再展示 11pt secondary 小字；支持自定义分隔线缩进、padding 与行高。
- Provider 列表收紧：Provider icon 从 28 pt 调到 24 pt，列表分隔线从文本区域起始，row vertical padding 收紧，开关改 `.controlSize(.mini)`。
- 通用页登录启动开关也改 `.controlSize(.mini)`。
- `PreferencesScene` 添加 `.toolbar(removing: .sidebarToggle)` 并保留 window toolbar hidden，进一步移除侧边栏收起按钮。
- 更新 `DESIGN.md` 偏好设置窗口视觉规范。
- 标记 `[0.3.0-PM-A-012]` 完成。

## 完成工作

- [x] 所有页面顶部显示页面标题（图标 + 标题）
- [x] 设置项说明文字改为分隔线下说明
- [x] Provider 等多对象列表更紧凑，分隔线缩进与文本区域对齐
- [x] 开关尺寸调小
- [x] 移除侧边栏收起按钮
- [x] 生成新 app：`quota-bar/build/20260630-010051-preferences-main/QuotaBar.app`

## 更新的需求 ID

- `[0.3.0-PM-A-012]` 新增并完成

## 更新的 README 或 DESIGN 章节

- `DESIGN.md` 新增「偏好设置窗口」章节，记录页面标题、说明文字分隔线、多对象列表、开关尺寸和侧边栏按钮规则。

## 验证方式

- `git pull`
- `cd quota-bar && swift build`
- `cd quota-bar && swift test`：20 tests in 4 suites passed
- `make app`：成功生成 `20260630-010051-preferences-main/QuotaBar.app`

## 备注

- 本轮保留了上一轮尚未提交的刷新间隔改动：`RefreshIntervalOption` 与通用页刷新间隔下拉框。它们与当前视觉调整同属 Preferences 页面工作线，最终提交时应一起整理提交说明。
