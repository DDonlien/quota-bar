# 用户原始 prompt

> 把这个四层表加入README.md，它值得以人类可阅读的形式记录作为后续开发的参考baseline

# 启动运行时的分支和版本

- 分支：`sub/main`
- 提交：`bc1ee47d8ad7a853618a539e0cf4ea9613fd7ec8`
- 已执行：本次对话前已执行 `git pull`，结果为 already up to date。

# 任务开始时间

2026-07-02 15:16 Asia/Shanghai

# 任务结束时间

2026-07-02 15:19 Asia/Shanghai

# 任务结束时是否执行了提交

未提交。

# 已阅读上下文

- `README.md`
- `REQUIREMENTS.md`

# 对话与行动记录

- 用户要求将四层获取矩阵加入 README，作为后续开发的人类可读 baseline。
- 在 README 的「支持的 Provider」章节后新增「四层获取矩阵（开发基线）」。
- 记录四层：Provider 获取、额度获取、过期日获取、档位与费用获取。
- 明确 provider 范围：Codex、Claude、Kimi、MiniMax、Antigravity、GLM / Z Code。
- 移除旧的“更多 provider”泛化表达，改为新增 provider 必须先明确具体名称与来源。

# 完成工作

- 更新 `README.md`，加入四张矩阵表和刷新来源索引/归一化说明。

# 更新的需求 ID

无新增。延续 `0.9.0-ARCH-B-*` 的文档化工作。

# 更新的 README 或 DESIGN 章节

- `README.md`：`支持的 Provider` 下新增 `四层获取矩阵（开发基线）`。

# 验证方式

- 使用 `sed` 复查 README 插入段落，确认 Markdown 表格结构正常。
- 未运行构建，因为本次只修改文档。

# 备注

- 工作区仍有多个未跟踪 agent-log 文件和既有 `REQUIREMENTS.md` 改动，未执行 git add/commit。
