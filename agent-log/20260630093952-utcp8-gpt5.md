# 2026-06-30 09:39 — Preferences macOS 26 外壳与对齐继续修正

## 用户原始 prompt

> 我才注意到，左侧的菜单栏为什么没有用 macOS 26 标准的卡片效果，而是整个嵌入的？这不对啊。参考附件。
>
> 此外，通用这个页面所有的文字和它里面的小字，应该是用横线分行的。
>
> 模型这个页面的横线两边应该顶到头，现在只有右边顶到头了
>
> 然后这个标题的尺寸和位置，应该和系统菜单栏右半部分那个“返回”和“前进”的按钮在同一个位置、对齐关系。

## 用户补充 prompt

> 我说错了，应该是右边也不到底
>
> 然后激活和关于页，对齐关系都检查一下，不是很标准（参考macos最佳实践）

## 启动运行时的分支和版本

- 分支：`preferences/main`
- HEAD：`358a2fb` refactor(preferences): 背景色从 .regularMaterial 玻璃改极浅灰（对齐软件更新页）
- 上游：`origin/feat/preferences`，本地 ahead 6
- 本轮沿用上一轮用户授权：此前 `git pull` 因 GitHub 443 连接超时失败，用户要求“忽略，直接改”。

## 任务开始时间

- 2026-06-30 09:32 (Asia/Shanghai, UTC+8)

## 任务结束时间

- 2026-06-30 09:39 (Asia/Shanghai, UTC+8)

## 任务结束时是否执行了提交

- 否

## 已阅读上下文

- 用户提供的系统设置参考截图
- `PreferencesScene.swift`
- `SettingsComponents.swift`
- `GeneralSettingsView.swift`
- `ModelsSettingsView.swift`
- `ActivationSettingsView.swift`
- `AboutSettingsView.swift`

## 对话与行动记录

- 将左侧 sidebar 包进独立圆角卡片：`regularMaterial` 背景、16pt 圆角、0.5pt separator 描边，整体在窗口内 inset。
- 将右侧 header 改为系统设置 toolbar 风格：返回 / 前进胶囊按钮占位 + 17pt semibold 页面标题，标题与按钮对齐。
- 通用页给所有带说明的设置行开启 `separatesSubtitle`：标题/控件行与说明行之间用横线分隔，横线保留左右 inset。
- 模型页分割线改为保留左右 inset：左侧从文本区域附近开始，右侧不顶到 group 边缘。
- 激活页统一 24pt 图标轨道，说明文字从文本列开始对齐。
- 关于页统一链接/维护行的图标轨道，app 信息行后的分割线从文本列开始，普通行保留 group 内 inset。
- 更新 `DESIGN.md` 和 `[0.3.0-PM-A-012]` 描述，记录最终视觉方向。

## 完成工作

- [x] 左侧 sidebar 卡片化
- [x] 右侧 header 改成 toolbar 行
- [x] 通用页标题/说明用横线分行
- [x] 模型页分割线两侧保留 inset
- [x] 激活 / 关于页对齐关系统一
- [x] 新 app：`quota-bar/build/20260630-093934-preferences-main/QuotaBar.app`

## 更新的需求 ID

- `[0.3.0-PM-A-012]`

## 更新的 README 或 DESIGN 章节

- `DESIGN.md`「偏好设置窗口」：更新 sidebar 卡片、toolbar 标题、通用页说明分割线、列表分割线 inset 等规则。

## 验证方式

- `cd quota-bar && swift build`
- `cd quota-bar && swift test`：20 tests in 4 suites passed
- `make app`：成功生成 `20260630-093934-preferences-main/QuotaBar.app`

## 备注

- 由于当前窗口外观高度依赖 macOS 26 的系统材质和 List sidebar 渲染，仍以用户实机截图作为最终视觉验收来源。
