# 2026-06-30 09:52 — Preferences 外观细节继续对齐 macOS 26

## 用户原始 prompt

> 你左侧的卡片完全没用swift（macos26）的标准组件啊，一点都不liquid glass，你也可以看看附件6的右半部分；右侧的标题（通用这些）位置是对的，返回/前进按钮不需要，有icon就行
> group的上下padding不对，参考一下附件2的系统菜单；
> 附件3的分割线左侧的padding应该和右侧一样
> 附件4的未激活没用分割线，说明移除
> 附件5的第一个分割线左边的padding和右侧不一致；重制按钮下没有分割线；链接移除，应用和链接的文字都移除，给我一个“检查更新”的按钮

## 用户补充 prompt

> 我说错了，应该是右边也不到底
>
> 然后激活和关于页，对齐关系都检查一下，不是很标准（参考macos最佳实践）

## 启动运行时的分支和版本

- 分支：`preferences/main`
- HEAD：`358a2fb` refactor(preferences): 背景色从 .regularMaterial 玻璃改极浅灰（对齐软件更新页）
- 上游：`origin/feat/preferences`，本地 ahead 6
- 本轮沿用之前用户授权：此前 `git pull` 因 GitHub 443 连接超时失败，用户要求“忽略，直接改”。

## 任务开始时间

- 2026-06-30 09:42 (Asia/Shanghai, UTC+8)

## 任务结束时间

- 2026-06-30 09:52 (Asia/Shanghai, UTC+8)

## 任务结束时是否执行了提交

- 否

## 已阅读上下文

- 用户提供的 6 张参考截图
- `PreferencesScene.swift`
- `SettingsComponents.swift`
- `GeneralSettingsView.swift`
- `ModelsSettingsView.swift`
- `ActivationSettingsView.swift`
- `AboutSettingsView.swift`

## 对话与行动记录

- 右侧 header 去掉返回/前进按钮，改为页面彩色 icon + 17pt semibold 标题。
- 左侧 sidebar card 改用 SwiftUI 系统 `.ultraThinMaterial`，增加玻璃边缘和柔和阴影。
- `SettingsRow` 默认 vertical padding 从 7 增到 9，改善 group 上下留白。
- 通用页保留标题/控件行与说明之间的横线，且横线在 row 内左右 inset。
- 模型页分割线改为左右同样 inset。
- 激活页「未激活」去掉额外说明，只保留状态行；说明区链接行也统一 24pt 图标轨道。
- 关于页移除「应用」「链接」section 标题，移除 GitHub/下载链接列表；新增「检查更新」按钮；重置偏好行增加标题/说明分割线。
- `DESIGN.md` 与 `[0.3.0-PM-A-012]` 同步最新视觉规则。

## 完成工作

- [x] Header：icon + title，无返回/前进按钮
- [x] Sidebar：系统材质 Liquid Glass 风格卡片
- [x] Group/Row：上下 padding 调整
- [x] 模型页：分割线左右 inset 一致
- [x] 激活页：移除未激活说明
- [x] 关于页：检查更新按钮、移除链接列表、重置说明分割线
- [x] 新 app：`quota-bar/build/20260630-095133-preferences-main/QuotaBar.app`

## 更新的需求 ID

- `[0.3.0-PM-A-012]`

## 更新的 README 或 DESIGN 章节

- `DESIGN.md`「偏好设置窗口」

## 验证方式

- `cd quota-bar && swift build`
- `cd quota-bar && swift test`：20 tests in 4 suites passed
- `make app`：成功生成 `20260630-095133-preferences-main/QuotaBar.app`

## 备注

- 关于页「检查更新」当前打开 GitHub Releases 页面，后续如需要可接入真正的更新检查服务。
