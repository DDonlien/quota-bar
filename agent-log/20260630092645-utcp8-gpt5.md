# 2026-06-30 09:26 — Preferences Header 固定与列表分割线修正

## 用户原始 prompt

> 首先，顶部的空间和对齐关系有一些问题。这个 Header 在右边的部分应该永远不动，参考右侧 Dynamic Island 的设计，并且它应该在顶部进行对齐，现在的对齐关系也不对，然后你新加的线，感觉 padding 也不太对，至少不是 macOS 26 标准实现，这个你检查一下，其他的元素和对齐关系也有类似的问题；模型这里问题最大。你已经用线区分模型和模型了，就不要在线和线之间再加一条两边不到顶的短线区分标题和正文了，看看附件 3，Mac 的系统自己是怎么做的。

## 启动运行时的分支和版本

- 分支：`preferences/main`
- HEAD：`358a2fb` refactor(preferences): 背景色从 .regularMaterial 玻璃改极浅灰（对齐软件更新页）
- 上游：`origin/feat/preferences`，本地 ahead 6
- `git pull`：失败，GitHub 443 连接超时；用户明确回复“忽略，直接改”，因此继续本地修改。

## 任务开始时间

- 2026-06-30 09:17 (Asia/Shanghai, UTC+8)

## 任务结束时间

- 2026-06-30 09:26 (Asia/Shanghai, UTC+8)

## 任务结束时是否执行了提交

- 否

## 已阅读上下文

- 用户提供的三张参考截图
- `SettingsComponents.swift`
- `ModelsSettingsView.swift`
- `GeneralSettingsView.swift`
- `REQUIREMENTS.md` PM-A 偏好设置需求区块
- 当前工作区 diff 与状态

## 对话与行动记录

- 定位到上一版问题：
  - `SettingsPageTitle` 被放在 `ScrollView` 内，导致右侧 header 跟随内容滚动，并可能与内容重叠。
  - `SettingsRow` 对所有 subtitle 都强制插入内部短分割线，导致 Provider 列表出现“对象分割线 + 标题/说明短线”的双重分割。
- 将 `SettingsPage` 改成固定 header + 独立 `ScrollView`：标题区域固定在 detail 顶部，不参与滚动。
- 将 `SettingsRow` 的 subtitle 恢复为系统设置列表行语义：标题与说明同属一个内容块，说明直接位于标题下方。
- Provider 列表保留对象之间的分割线，移除标题与说明之间的内部短线。
- 更新 `REQUIREMENTS.md` / `DESIGN.md` 对视觉规则的描述，避免继续记录错误的“说明必须先加短线”。

## 完成工作

- [x] 右侧 detail header 固定在顶部，不随 ScrollView 滚动
- [x] Provider 列表移除标题与说明之间的短线，只保留不同 Provider 之间的分割线
- [x] 说明文字规则改为 macOS 系统设置列表行语义
- [x] 将 Preferences 根容器从 `NavigationSplitView` 改为固定 sidebar + detail split，避开系统自动插入的 sidebar toggle
- [x] 新 app：`quota-bar/build/20260630-092825-preferences-main/QuotaBar.app`

## 更新的需求 ID

- `[0.3.0-PM-A-012]` 文案修正：固定 header、行与行之间分割，标题与说明不额外插线。

## 更新的 README 或 DESIGN 章节

- `DESIGN.md`「偏好设置窗口」：修正 detail header 固定规则、说明文字与分割线规则。

## 验证方式

- `cd quota-bar && swift build`
- `cd quota-bar && swift test`：20 tests in 4 suites passed
- `make app`：成功生成 `20260630-092825-preferences-main/QuotaBar.app`

## 备注

- `NavigationSplitView` 会在部分系统状态下自动插入 sidebar toggle；本轮改用固定 split 布局规避该行为。
