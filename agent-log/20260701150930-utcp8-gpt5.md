# 用户原始 prompt

你的新版本直接刷新不出kimi的过期信息了？

# 启动运行时的分支和版本

- 分支：`sub/expiry`
- 版本：`14945f6fc0973cba3d2b60a342472ca77284c626`
- `git pull`：已执行，结果 `Already up to date.`

# 任务开始时间

2026-07-01 15:03:00 +0800

# 任务结束时间

2026-07-01 15:09:30 +0800

# 任务结束时是否执行了提交

否

# 已阅读上下文

- `AGENTS.md`
- `README.md`
- `REQUIREMENTS.md`
- `DESIGN.md`
- `~/Library/Logs/QuotaBar.log`
- `quota-bar/Sources/QuotaBar/SubscriptionExpirySources.swift`
- `quota-bar/Sources/QuotaBar/BrowserCookieProvider.swift`
- `quota-bar/Sources/QuotaBar/DashboardEndpoints.swift`
- `quota-bar/Sources/QuotaBar/KimiHarvester.swift`
- `quota-bar/Tests/QuotaBarTests/KimiSubscriptionStatParserTests.swift`

# 对话与行动记录

- 查看 `QuotaBar.log`，确认新版 Kimi 刷新时主额度 Cookie API 正常返回 tier/price，但 `kimi-membership-page` headless DOM source 多次 `extract returned nil`，导致过期日隐藏。
- 判断上一版把 Kimi 过期日完全依赖 membership 页过于脆弱。
- 恢复 `KimiSubscriptionStatParser.parseSubscriptionExpiresAt(data:)` 从 `subscriptionBalance.expireTime` 返回原始日期，但不把该值放进 Work quota 的 `resetsAt`。
- 在 `BrowserCookieProvider.fetchKimiSnapshot` 中将 Kimi API 原始日期按 `.nextRenewalDate` 语义转换为最后有效日后写入 `ProviderSnapshot.subscriptionExpiresAt`，source 标记为 `.browserAPI` / `.medium`。
- 保留 membership 页 headless source 作为后备，不再作为 Kimi 唯一过期日来源。
- 更新 Kimi parser 测试与需求记录。

# 完成工作

- 修复 Kimi 新版本刷新不出过期信息的问题。
- Kimi 日期链路现在为：`GetSubscriptionStat.subscriptionBalance.expireTime` 原始续费日 → 本地自然日减 1 → UI 展示最后有效日。
- Work quota 仍不使用 `expireTime` 作为 `resetsAt`，避免额度 reset 和订阅日混淆。
- 重新打包：`quota-bar/build/20260701-150910/QuotaBar.app`，`quota-bar/build/latest/QuotaBar.app` 已指向最新包。

# 更新的需求 ID

- `[0.6.0-ARCH-A-003]`
- `[0.6.0-DATA-A-001]`
- `[0.6.0-DATA-A-002-test]`
- `[0.6.0-DATA-A-003]`

# 更新的 README 或 DESIGN 章节

无

# 验证方式

- `swift test`：通过，64 tests in 11 suites passed。
- `git diff --check`：通过。
- `./scripts/build-app.sh`：通过，生成 `build/20260701-150910/QuotaBar.app`。

# 备注

- Codex / Antigravity 日志仍显示 headless DOM 页面能加载但提取不到日期；本次只修复用户指出的 Kimi 新版回退问题。
