# 用户原始 prompt

> 现在应该可以merge了

上下文：用户上一轮要求把 `sub/main` 以及 `sub/expiry` 最新内容 merge 到 `main`，并明确流程为先读代码了解区别，使用 `git merge` 保存 Git 记录，然后在 `main` 上修改冲突和不需要的部分。

# 启动运行时的分支和版本

- 分支：`main`
- 版本：`680f2b2cf7004b322aa180ed9e6961d42bd88df5`
- 同步预检：`GIT_TERMINAL_PROMPT=0 git fetch --prune` 通过；`main`、`sub/main`、`sub/expiry` 均与各自 upstream 对齐。

# 任务开始时间

2026-07-03 21:30:56 +0800

# 任务结束时间

2026-07-03 21:49:55 +0800

# 任务结束时是否执行了提交

是：`sub/expiry` merge commit 已创建；`sub/main` merge commit 在本日志暂存后创建。

# 已阅读上下文

- 根目录 `AGENTS.md`
- `main` / `sub/main` / `sub/expiry` 工作区状态
- `sub/main` 最新提交日志与差异
- `sub/expiry` 最新提交日志与差异
- `sub/main` 最新执行日志：
  - `agent-log/20260702013307-utcp8-mavis.md`
  - `agent-log/20260702170822-utcp8-gpt5.md`
  - `agent-log/20260702215620-utcp8-gpt5.md`
  - `agent-log/20260702223030-utcp8-gpt5.md`
  - `agent-log/20260703190832-utcp8-gpt5.md`
  - `agent-log/20260703191614-utcp8-gpt5.md`
- `sub/expiry` 与上次已合入内容对比。

# 对话与行动记录

- 重新检查后确认 `main`、`sub/main`、`sub/expiry` 当前均为干净工作区。
- `sub/expiry` 没有新的提交，内容已经在上一轮按功能迁移到 `main`，但此前没有 Git merge 记录；本轮需要补真实 merge 拓扑。
- `sub/main` 已推进到 `2932f14`，包含三层获取架构、持久化缓存、Kimi Desktop token、Z Code、WebView 授权入口、Antigravity RPC/CLI 顺序和菜单栏状态修正。
- 由于 `sub/main` / `sub/expiry` 仍基于旧 `quota-bar/` 目录，merge 后需要在 `main` 上整理为当前 `macos/` 结构，最终不保留旧目录。
- 执行 `git merge --no-ff -s ours sub/expiry`，为已合入的 `sub/expiry` 内容补真实 merge 拓扑。
- 执行 `git merge --no-ff --no-commit sub/main`，解决旧 `quota-bar/` → 当前 `macos/` 结构的路径冲突。
- 以 `sub/main` 的 Provider 管线为整块来源并入：Provider source index、last-known-good snapshot cache、Kimi Desktop token、Z Code billing balance / plan cache、WebView 授权入口、Antigravity RPC→CLI→Keychain 顺序、Codex inspector 不再预先短路、needsConfiguration 不再绘制菜单栏占位 bar。
- 保留 `main` 已有能力：`macos/` / `site/` 目录结构、Preferences 页面、GLM provider 类型、`sub/expiry` 的独立订阅到期日 source resolver 与 source/confidence metadata。
- 修复合并后的 Swift 枚举和缓存 Codable 问题：补 `ProviderKind.zcode` 的 Preferences 展示；让订阅到期日 source enum 可编码；Provider snapshot cache 保留过期日来源 metadata。

# 完成工作

- `sub/expiry` 已作为真实 merge commit 进入 `main`。
- `sub/main` 已进入正在收尾的 merge commit，代码合并到 `main` 的 `macos/` 结构。
- `REQUIREMENTS.md` 中已存在并保留 `0.10.0-DATA-B-*` 后续需求，记录“所有 AI provider 都可能有免费额度 / 订阅状态误判，后续需要统一订阅状态识别；本次 merge 不扩展处理”。
- 未恢复旧 `quota-bar/` 目录。

# 更新的需求 ID

- 已确认并保留：`0.10.0-DATA-B-000`、`0.10.0-DATA-B-001`、`0.10.0-DATA-B-002`、`0.10.0-DATA-B-003`、`0.10.0-QA-B-000`

# 更新的 README 或 DESIGN 章节

- `README.md` 更新 Provider 支持说明、四层获取矩阵、持久化与默认不启用浏览器 Cookie 的 TCC 说明。
- `DESIGN.md` 未更新。

# 验证方式

- `cd macos && env CLANG_MODULE_CACHE_PATH=.build/clang-module-cache SWIFT_MODULE_CACHE_PATH=.build/swift-module-cache swift test --disable-sandbox` 通过，109 个 Swift Testing 用例通过。

# 备注

- 本次按用户要求保留 Git merge 记录，不再用普通移植提交替代。
- SwiftPM 输出了用户级 cache 不可写 warning，但构建和测试均完成通过。
