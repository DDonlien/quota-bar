# 2026-06-30 订阅过期日独立 source pipeline 实现

## 用户原始 prompt

> 可以的，按你说的实现，然后我帮你补上浏览器信息：
> 为你补充第三层的信息：
> - claude：https://claude.ai/new#settings/billing
> - codex：https://chatgpt.com/#settings/Billing
> - minimax：https://platform.minimaxi.com/console/plan
> - kimi：https://www.kimi.com/membership/subscription?tab=quota
> - glm：还没订阅，不确定
>
> 另外，过期日和额度信息不冲突。因为有些服务在我本地可以安装，安装完以后即使我没有任何订阅，也有可能会有一些免费的额度，就如实显示就好。

## 启动运行时的分支和版本

- 分支：`sub/expiry`
- `git pull`：Already up to date
- HEAD：`14945f6fc0973cba3d2b60a342472ca77284c626`
- 开始时已有未提交日志：
  - `agent-log/20260630123525-utcp8-gpt5.md`
  - `agent-log/20260630124434-utcp8-gpt5.md`

## 任务开始时间

2026-06-30 12:48 (UTC+8)

## 任务结束时间

2026-06-30 13:03 (UTC+8)

## 任务结束时是否执行了提交

否

## 已阅读上下文

- `AGENTS.md`
- `REQUIREMENTS.md` v0.6.0 / v0.7.0 相关段落
- `quota-bar/Sources/QuotaBar/Strategies.swift`
- `quota-bar/Sources/QuotaBar/RefreshCoordinator.swift`
- `quota-bar/Sources/QuotaBar/SubscriptionDateHarvester.swift`
- `quota-bar/Sources/QuotaBar/WKWebViewHeadlessLoader.swift`
- `quota-bar/Sources/QuotaBar/Harvesters.swift`
- `quota-bar/Sources/QuotaBar/*Harvester.swift`
- harvester / loader 相关测试

## 对话与行动记录

1. 按规范确认分支和 HEAD，执行 `git pull`；首次被 worktree git 元数据权限拦截，授权后成功，远端已最新。
2. 将用户关于“过期日与额度不冲突”和确认的浏览器订阅页入口登记到 `REQUIREMENTS.md` 的 `ARCH-B：订阅过期日独立数据源管线`。
3. 新增 `SubscriptionExpirySourceKind` / `SubscriptionExpiryConfidence` / `SubscriptionExpirySource` / `SubscriptionExpiryResolver`，把过期日从旧的 `enrichWithHarvester` 改成独立 source pipeline。
4. 在 `ProviderSnapshot` 上增加 `subscriptionExpiresAtSource` 和 `subscriptionExpiresAtConfidence` 调试元数据。
5. 更新 `RefreshCoordinator`：quota snapshot 成功后调用 `enrichWithSubscriptionExpiry`；过期日失败只隐藏日期，不改变 `availability` / `quotas`。
6. 更新 headless DOM URL：
   - Codex：`https://chatgpt.com/#settings/Billing`
   - Claude：`https://claude.ai/new#settings/billing`
   - MiniMax：`https://platform.minimaxi.com/console/plan`
   - Kimi：新增 `KimiHarvester`，作为 API 后的 membership 页兜底
7. `WKWebViewHeadlessLoader` 增加显式 `cookieDomains` 参数，支持订阅页域和额度 API 域不同。
8. `Harvesters` 改为 `SubscriptionExpirySources` 的兼容 facade，避免两套注册表分叉。
9. 新增 `SubscriptionExpirySourcesTests`，覆盖 source 顺序、用户确认 URL、Kimi API 优先、免费额度无过期日仍保持 available。

## 完成工作

- [x] 订阅过期日 source pipeline 抽象落地。
- [x] 过期日和额度状态解耦。
- [x] 用户提供的浏览器订阅页 URL 已接入。
- [x] Kimi API 优先 + headless membership 页兜底。
- [x] 测试文件已更新 / 新增。

## 更新的需求 ID

- [0.6.0-ARCH-B-000]
- [0.6.0-ARCH-B-001]
- [0.6.0-ARCH-B-002]
- [0.6.0-ARCH-B-003-test]

## 更新的 README 或 DESIGN 章节

- 无。此次为内部数据源架构与 URL 更新，不改变 README / DESIGN 的用户入口说明。

## 验证方式

- `git diff --check`：通过。
- `swift test`：
  - 第一次在沙箱内失败，原因是 SwiftPM / Clang 需要写 `~/.cache/clang/ModuleCache`。
  - 授权重跑后进入编译，发现新测试里 `refreshDescription: nil` 类型错误和 `.available` 推断问题；已修复。
  - 再次授权重跑被审批层拒绝（提示 usage limit / 需稍后再试），因此本轮未能完成完整测试验证。
  - 2026-06-30 23:37 继续重跑：`swift test` 通过，63 tests in 11 suites passed。

## 备注

- 当前代码静态检查干净，完整 `swift test` 已于后续重跑通过。
- GLM 因用户未订阅且入口不确定，未接入过期日 source。
