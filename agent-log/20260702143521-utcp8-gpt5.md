# 用户原始 prompt

> /Users/taobe/Projects/GitHub/Reference
> 看看这三个参考项目，看看他们是怎么实现额度获取的
>
> 另外：即使只是按需的时候跳弹窗，要求输入密码并允许从而获取 cookie，这个体验也非常非常差。
>
> 我们要么找到别的方法，不跳弹窗，只通过长期的系统设置就能获取到 cookie；要么找别的 work around，也就是不依赖这个浏览器 cookie 的获取
>
> 但在那之前，我们要先解决额度本身准确获取的问题。
>
> 这个 cookie 如果只和过期日有关，你就先别管；如果和别的也有关，就一起看。

# 启动运行时的分支和版本

- 分支：`sub/main`
- 提交：`bc1ee47d8ad7a853618a539e0cf4ea9613fd7ec8`
- 已执行：`git pull`，结果为 already up to date。

# 任务开始时间

2026-07-02 14:18 Asia/Shanghai

# 任务结束时间

2026-07-02 14:35 Asia/Shanghai

# 任务结束时是否执行了提交

未提交。

# 已阅读上下文

- `AGENTS.md`
- `README.md`
- `REQUIREMENTS.md`
- `DESIGN.md`
- `agent-log/20260702141144-utcp8-gpt5.md`
- 参考项目：
  - `/Users/taobe/Projects/GitHub/Reference/CodexBar`
  - `/Users/taobe/Projects/GitHub/Reference/ClaudeBar`
  - `/Users/taobe/Projects/GitHub/Reference/Claude-Usage-Tracker`

# 对话与行动记录

- 检查三个参考项目的 README 和关键源码，重点关注额度获取、订阅状态来源、cookie/Keychain 权限处理。
- 对比 Codex 获取策略：CodexBar 将 OpenAI web/cookie 作为可选 extras；ClaudeBar 的 Codex API probe 直接读取 `~/.codex/auth.json` 的 OAuth token，必要时刷新 token，然后调用 `https://chatgpt.com/backend-api/wham/usage`，不依赖 `id_token` 的订阅字段。
- 对比 Claude 获取策略：ClaudeBar 和 Claude-Usage-Tracker 都优先使用 Claude Code CLI/OAuth 凭证或 Claude.ai session key；Claude-Usage-Tracker 还提供嵌入式 WKWebView 登录捕获 sessionKey 的路径，避免读取系统浏览器 cookie。
- 对比 Kimi 获取策略：ClaudeBar 提供 CLI `/usage` 模式作为推荐路径，也提供 `KIMI_AUTH_TOKEN` 环境变量绕过 cookie；浏览器 cookie 是 API 模式的可选来源。
- 检查 CodexBar 的 Keychain/browser cookie gate：有预检、冷却、禁用 Keychain、默认减少浏览器扫描和手动 cookie header 存储等机制，用于减少弹窗，而不是把弹窗作为主流程。

# 完成工作

- 得出产品策略建议：先解决 Codex 额度准确性，不应把 `id_token` 的订阅过期字段作为当前真相；Codex 额度应优先走 OAuth access/refresh token 或 Codex RPC/CLI，浏览器 cookie 降级为可选补充。
- 得出权限策略建议：Full Disk Access 只能解决读取某些文件路径的问题，不能优雅消除 Chromium cookie 解密需要访问 Keychain safe storage 的系统弹窗；最佳 workaround 是避免系统浏览器 cookie 作为核心路径，改用 CLI/OAuth、本应用内登录、手动 token/header 或 provider 官方/半官方接口。

# 更新的需求 ID

无。本次只做调查与方案判断。

# 更新的 README 或 DESIGN 章节

无。

# 验证方式

- 使用 `rg`、`find`、`sed` 阅读三个参考项目的 README 和关键 Swift 源码。
- 未运行本项目构建或测试，因为本次未修改实现代码。

# 备注

- 当前工作区已有未跟踪日志文件 `agent-log/20260702141144-utcp8-gpt5.md`，本次未处理其 git 状态。
