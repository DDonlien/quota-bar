# 用户原始 prompt

> 你改就改了，为什么把左边本来好了的侧栏又改错了？？？？？

补充：

> 刚刚图发错了

补充：

> 我试了一下，要交互一次之后才会变正确，但这个更奇怪了

补充：

> header和第一个选项之间的间隔缩小，保持选项和选项（比如刷新的group和数据来源）之间一样就好

# 启动运行时的分支和版本

- 分支：`preferences/main`
- 提交：`358a2fb`
- 备注：延续偏好设置 UI 调整任务，当前工作区已有未提交改动；本轮未执行 `git pull`。

# 任务开始时间

- 2026-07-01 00:18:00 +0800

# 任务结束时间

- 2026-07-01 00:21:55 +0800

# 任务结束时是否执行了提交

- 否。

# 已阅读上下文

- `quota-bar/Sources/QuotaBar/Preferences/PreferencesWindowController.swift`
- `quota-bar/Sources/QuotaBar/Preferences/PreferencesScene.swift`
- `quota-bar/Sources/QuotaBar/Preferences/SettingsComponents.swift`
- `quota-bar/Sources/QuotaBar/Preferences/GeneralSettingsView.swift`

# 对话与行动记录

- 用户反馈左侧 sidebar 初始状态错误，交互一次后才恢复，说明前序隐藏 sidebar toggle 的 AppKit 兜底影响了 SwiftUI 原生 sidebar 初始布局。
- 撤销 `PreferencesWindowController` 中创建隐藏 toolbar、递归隐藏 sidebar toggle button 的所有逻辑。
- 保留 SwiftUI `NavigationSplitView` + `List(.sidebar)` 原生 sidebar 路线，避免再次污染左侧初始布局。
- 收紧 `SettingsPage` 的滚动内容顶部 inset：从 `headerHeight + sectionSpacing` 改为 `headerTopInset + headerContentHeight + sectionSpacing`，让 header 标题行到第一个 section 的距离接近 section 之间的距离。

# 完成工作

- 左侧 sidebar 不再被 AppKit 递归隐藏逻辑干扰，恢复原生初始布局。
- 右侧 header 与第一个 section 的视觉间距缩小。
- 重新生成本地 `.app`。

# 更新的需求 ID

- 延续偏好设置 UI polish 相关需求；本轮未新增独立需求 ID。

# 更新的 README 或 DESIGN 章节

- 无。

# 验证方式

- `cd quota-bar && swift build`：通过。
- `cd quota-bar && swift test`：通过，20 个测试全部通过。
- `make app`：通过，产物为 `quota-bar/build/20260701-002140-preferences-main/QuotaBar.app`。

# 备注

- 当前选择是优先保证原生 sidebar 正确渲染；不再使用会影响 SwiftUI 初始布局的 AppKit 递归隐藏方案。
