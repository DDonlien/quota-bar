# 用户原始 prompt

> 关于页面，把build dev改成yymmdd.hhmmss.<branchname>
> title的上对齐，不对，比如这里的关于，应该横向和红绿灯、收起菜单按钮对齐
> 收起菜单按钮没有移除

补充：

> header的标签（图1）现在有一个实心的遮罩，但道理应该是fadeout半透明的，参考Macos26最佳实践或者vibesiland（图2）

# 启动运行时的分支和版本

- 分支：`preferences/main`
- 提交：`358a2fb`
- 备注：延续同一偏好设置 UI 调整任务，当前工作区已有未提交改动；按用户前序要求继续直接修改，未执行 `git pull`。

# 任务开始时间

- 2026-06-30 23:34:00 +0800

# 任务结束时间

- 2026-06-30 23:41:35 +0800

# 任务结束时是否执行了提交

- 否。

# 已阅读上下文

- `quota-bar/Sources/QuotaBar/Preferences/AboutSettingsView.swift`
- `quota-bar/Sources/QuotaBar/Preferences/SettingsComponents.swift`
- `quota-bar/Sources/QuotaBar/Preferences/PreferencesWindowController.swift`
- `quota-bar/Sources/QuotaBar/Preferences/PreferencesScene.swift`
- `quota-bar/scripts/build-app.sh`
- `DESIGN.md`

# 对话与行动记录

- 用户反馈关于页仍显示 `Build dev`、右侧 title 顶部对齐不符合 macOS 26 系统设置、侧边栏收起按钮仍未移除。
- 用户随后补充：固定 header 当前像实心遮罩，滚动内容应在顶部 header 下方通过半透明 fadeout 过渡。
- 修改打包脚本，将 `CFBundleVersion` 写为 `yymmdd.hhmmss.<branchname>`，并修正原脚本中 Info.plist 写入顺序和 `CFBundleVersion` 占位值。
- 将 `SettingsPage` 从「固定实心标题区 + ScrollView」改为 `ZStack`：内容从顶部滚动，header 作为覆盖层，使用 `.bar` 材质和 LinearGradient mask 做向下渐隐。
- 让右侧 header 忽略 detail 顶部 safe area，使标题位置更贴近 titlebar / 红绿灯所在行。
- 在 `PreferencesWindowController` 增加 AppKit 兜底，窗口展示后递归隐藏 accessibility label / identifier 中包含 sidebar / 边栏 / 收起 的非红绿灯按钮。
- 更新 `DESIGN.md` 中 header fade 和 build 号格式规范。

# 完成工作

- 关于页 build 号随 `.app` 打包写入 bundle，格式为 `260630.234111.preferences-main` 这类。
- 右侧 header 改为半透明材质渐隐覆盖层，不再用实心遮罩硬切滚动内容。
- 右侧 header 位置更接近 titlebar 顶部行。
- 增加 sidebar toggle 隐藏兜底逻辑。

# 更新的需求 ID

- 延续偏好设置 UI polish 相关需求；本轮未新增独立需求 ID。

# 更新的 README 或 DESIGN 章节

- 更新 `DESIGN.md` 的「偏好设置窗口」章节。

# 验证方式

- `cd quota-bar && swift build`：通过。
- `cd quota-bar && swift test`：通过，20 个测试全部通过。
- `make app`：通过，产物为 `quota-bar/build/20260630-234111-preferences-main/QuotaBar.app`。
- `/usr/libexec/PlistBuddy -c 'Print :CFBundleVersion' quota-bar/build/latest/QuotaBar.app/Contents/Info.plist`：输出 `260630.234111.preferences-main`。

# 备注

- 若 macOS 26 某个系统 build 的 sidebar toggle accessibility 文案不同，仍可能需要补充匹配条件；当前策略避免按坐标遮盖，优先不破坏原生 sidebar 视觉。
