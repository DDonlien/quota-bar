# 2026-06-30 截图诊断：Kimi 过期日误判与 headless 报错可观测性

## 用户原始 prompt

> 结果是这样，相当于Kimi还是错的，然后其他的都没获取到，为什么？能看到报错吗？

截图显示：
- Codex / MiniMax / Antigravity 未显示订阅日期；
- Kimi 显示 `2026/7/10`，但 Work 月额度剩余 `9d8h`，高度怀疑该日期是额度周期截止而不是订阅续费日。

## 启动运行时的分支和版本

- 分支：`sub/expiry`
- HEAD：`14945f6fc0973cba3d2b60a342472ca77284c626`
- 本轮开始前已有未提交的订阅过期日 source pipeline 改动和 agent logs。

## 任务开始时间

2026-06-30 23:47 (UTC+8)

## 任务结束时间

2026-06-30 23:51 (UTC+8)

## 任务结束时是否执行了提交

否

## 已阅读上下文

- 用户截图
- macOS unified log（`/usr/bin/log show`）
- `quota-bar/Sources/QuotaBar/DashboardEndpoints.swift`
- `quota-bar/Sources/QuotaBar/BrowserCookieProvider.swift`
- `quota-bar/Sources/QuotaBar/KimiAuthProvider.swift`
- `quota-bar/Sources/QuotaBar/SubscriptionExpirySources.swift`
- Kimi / source pipeline 相关测试

## 对话与行动记录

1. 读取 macOS unified log。能看到大量 WebKit/WebContent sandbox / bootstrap lookup 报错，但业务 `NSLog("QuotaBar: ...")` 在 unified log 中不稳定，不适合作为用户调试入口。
2. 确认 Kimi 误判根因：代码仍把 `GetSubscriptionStat.subscriptionBalance.expireTime` 当 `subscriptionExpiresAt`。截图中的 `2026/7/10` 与 Work 月额度 `9d8h` 对齐，说明它更像余额/周期截止。
3. 修复 Kimi：
   - `KimiAuthProvider` 不再追加 `GetSubscriptionStat` 来填订阅日。
   - `BrowserCookieProvider.fetchKimiSnapshot` 不再把 `parseSubscriptionExpiresAt` 塞进 `ProviderSnapshot.subscriptionExpiresAt`。
   - `KimiSubscriptionStatParser.parseSubscriptionExpiresAt` 改为返回 nil。
   - `REQUIREMENTS.md` 标记旧的 Kimi expireTime 方案为 `#cut`，新增完成项记录修正。
4. 加可观测性：
   - 新增 `QuotaBarDiagnostics`，写入 `~/Library/Logs/QuotaBar.log`。
   - `SubscriptionExpiryResolver` 的 source start / extract nil / parsed / failed 都写本地日志。
5. 重新运行测试并打包。

## 完成工作

- [x] 修正 Kimi 过期日误判。
- [x] 订阅过期日 source 失败写入 `~/Library/Logs/QuotaBar.log`。
- [x] `swift test` 通过。
- [x] 重新打包。

## 更新的需求 ID

- [0.6.0-DATA-A-001] 标记 `#cut`
- [0.6.0-DATA-A-002]
- [0.6.0-DATA-A-002-test]
- [0.6.0-DATA-A-003]

## 更新的 README 或 DESIGN 章节

- 无。

## 验证方式

- `git diff --check`：通过。
- `swift test`：63 tests in 11 suites passed。
- `./scripts/build-app.sh`：Build complete。
- 最新包：`quota-bar/build/20260630-235147/QuotaBar.app`

## 备注

- 截图中其他 provider 没有日期，当前系统日志显示 WebKit/WebContent 层有 sandbox / bootstrap lookup 报错；下一次运行新包后，应直接查看 `~/Library/Logs/QuotaBar.log` 来确认每个 source 是 cookie 缺失、加载失败、超时，还是 DOM extract nil。
