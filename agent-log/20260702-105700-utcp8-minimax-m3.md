# 20260702-105700-utcp8-minimax-m3

## 用户原始 prompt

> 帮我增加一个功能到应用：现在的检查更新是假的，我需要是真的，先给我点建议：是从 github 还是从 vercel 获取最新的包？

> 引入 smver，保留 nightly 作为常规的打包、推送，如果需要 smver 更新版本单独调用另一个打包指令并在 commit 或者指令中写入版本号
> B
> 我不知道你说的鉴权是什么？更新后我希望 app 能保留之前所有的系统权限和设置的

> 多问一句，签名要怎么获得？

> 加入 requirements

## 启动运行时分支与版本

- 分支：`main`
- 提交：`c6317d0 build(app): package latest main app`
- 工作区：项目根目录（`/Users/taobe/Projects/GitHub/Personal/quota-bar`）
- 未提交改动：仅 site/ 侧（与本任务无关）
- 未拉取改动：本地 = upstream，无 behind/ahead

## 任务开始时间

2026-07-02 10:51 (Asia/Shanghai, UTC+8)

## 任务结束时间

2026-07-02 10:57 (Asia/Shanghai, UTC+8)

## 任务结束时是否执行了提交

否。**仅更新 REQUIREMENTS.md，未要求 commit / push**。

## 已阅读上下文

- 根 `AGENTS.md`（协作规范、目录命名、worktree 模式）
- 根 `REQUIREMENTS.md`（读到 v0.10.0 末尾，确认风格 / 命名 / 稳定 ID 规则）
- `git status` / `branch -a` / `worktree/` 列表（确认 main + 现有 worktree 命名习惯）
- 之前对话中已确认的现状：
  - `release.yml:124` 注释 `unsigned and not notarized`
  - `build-app.sh:48` 写死 `com.taobe.quotabar`
  - `build-app.sh:78` 已有 `--identifier com.taobe.quotabar` 重签名
  - `AboutSettingsView` 的「检查更新」是占位
  - `CFBundleShortVersionString` 硬编码 `"1.0"`
  - `CFBundleVersion` / `QBDisplayBuild` 是日期戳 `YYMMDD.HHMMSS.branch`

## 对话与行动记录

### 第 1 轮：选 update source

用户问：检查更新走 GitHub 还是 Vercel？

Agent 答：走 **GitHub Releases**。理由：
- Vercel 只跑 `site/`，不发布 macOS app
- API 公开、零新基础设施
- 60 req/IP/h 限流够用
- 已经是「检查更新」按钮实际指向的页面

提出 3 个岔路口让用户拍：
1. 版本号比法（A nightly + 日期戳 / B semver）
2. UX 形态（A 检测+跳转 / B 下载+helper / C Sparkle 完整方案）
3. 是否鉴权

### 第 2 轮：拍方案

用户：
- 引入 semver，保留 nightly 常规打包
- 选 **B**（下载 + helper 替换）
- 不理解「鉴权」（指 GitHub API 鉴权，与 macOS TCC 权限无关 —— Agent 误用了术语，向用户道歉）
- 强调「更新后希望 app 能保留之前所有的系统权限和设置」

Agent 答：方案 B 可行，但需要：
1. helper 进程做替换（app 不能运行时替换自己）
2. 签名 + notarize 当前缺失
3. bundle id / signature identifier 必须稳定（`com.taobe.quotabar` 已是）
4. 给用户两个执行路径建议

### 第 3 轮：拍签名

用户问：签名要怎么获得？

Agent 答：
- Developer ID Application cert（Apple Developer Program $99/年）
- 本地签 vs CI 签，建议本地签
- notarize 用 `xcrun notarytool` + App Store Connect API Key
- 实操路径：enroll → 生成 cert → 改 `build-app.sh` → 加 notarize 步骤
- 隐藏坑：helper 脚本本身也要签名 + notarize

### 第 4 轮：写入 requirements

用户说：加入 requirements。

Agent 行动：
1. 读 `REQUIREMENTS.md` 全量（v0.0.0 ~ v0.10.0）和 `AGENTS.md` 全量
2. 确认命名风格、ID 规则、phase 结构
3. 确认现有 worktree 命名习惯（`dropdown/main`、`menubar/main`、`preferences/main`、`site/main`、`sub/main`、`sub/expiry`）
4. 提议新增 v0.11.0 phase，worktree 名 `update/main`（与现有 `dropdown/main` 等对齐）
5. 用 Edit 追加 v0.11.0 phase 到 REQUIREMENTS.md 末尾

## 完成工作

- [x] 在 `REQUIREMENTS.md` 末尾追加 v0.11.0 phase（共 7 个子节、约 50 条 task）
- [x] 本日志

## 更新的需求 ID

新增 phase：

- **Phase - v0.11.0 - 真实自动更新 + semver 发版 + 代码签名公证**
  - `update/main: 引入 semver 发版路径`（CI-A-000 ~ 004 + 1 test）
  - `update/main: 接入 Apple Developer ID 签名 + notarize`（SEC-A-000 blocked ~ 004 + 3 tests）
  - `update/main: 写轻量 helper 替换脚本`（TOOL-A-000 ~ 005 + 2 tests）
  - `update/main: 实现 UpdateChecker（GitHub Releases API）`（FE-A-000 ~ 007 + 2 tests）
  - `update/main: 后台下载 + 提示重启安装`（FE-A-008 ~ 012 + 2 tests）
  - `update/main: 更新检查 UI 改造`（UI-A-000 ~ 004 + 1 test）
  - `update/main: 更新流程文档与验收`（DOC-A-000 ~ 002 + QA-A-000 ~ 005）

稳定 ID 分配：

- `[0.11.0-CI-A-000]` ~ `[0.11.0-CI-A-004]` + 1 test
- `[0.11.0-SEC-A-000]` ~ `[0.11.0-SEC-A-004]` + 3 tests
- `[0.11.0-TOOL-A-000]` ~ `[0.11.0-TOOL-A-005]` + 2 tests
- `[0.11.0-FE-A-000]` ~ `[0.11.0-FE-A-012]` + 4 tests
- `[0.11.0-UI-A-000]` ~ `[0.11.0-UI-A-004]` + 1 test
- `[0.11.0-DOC-A-000]` ~ `[0.11.0-DOC-A-002]`
- `[0.11.0-QA-A-000]` ~ `[0.11.0-QA-A-005]`

## 更新的 README 或 DESIGN 章节

未更新。本 phase 的 `DOC-A-000` / `DOC-A-001` / `DOC-A-002` 任务在 v0.11.0 落地时才会改 README / DESIGN / macos/AGENTS.md；本次只登记需求。

## 验证方式

- [x] `git diff REQUIREMENTS.md` 末尾追加 v0.11.0 phase，未影响既有 phase
- [x] 所有 task ID 走 `[0.11.0-*-A-NNN]` 格式，`<parent>-test` 模式正确
- [x] 未触碰 README / DESIGN / AGENTS.md / 任何代码文件
- [x] 严格遵守"任务不进顶层 meta-check block" / "测试任务为子任务" 等用户偏好
- [x] 命名规则：`## Phase - vX.Y.Z - description` + `### branch-name: feature description`
- [x] 未提交

## 备注

- **worktree 拆分决策**：所有任务集中在 `update/main` 单 worktree，因为：
  1. semver / 签名 / helper / UpdateChecker / UI 是同一交付节奏
  2. helper 强依赖 SEC 段（签名脚本），强行拆开 worktree 反而割裂
  3. 现有 worktree 模式（`dropdown/main` `menubar/main` 等）都是一 feature 一 worktree
  - 后续如果需要把 helper 拆成独立二进制 + 独立签名链，再单独拆 worktree
- **命名对齐**：proposed `update/main` 与现有 `dropdown/main` / `menubar/main` / `preferences/main` / `site/main` / `sub/main` / `sub/expiry` 对齐
- **关键依赖**：`SEC-A-000`（用户 enroll Apple Developer）阻塞所有 helper 任务（TOOL 段）和 UpdateChecker 落地（FE 段需要真实签名 dmg 才能 verify）
- **隐含 bug 修复**：v0.11.0-SEC-A-004 显式断言 bundle id / signature identifier 不变 —— 这是「TCC 权限更新后保留」承诺的形式化保障
- **未走 ask_user**：用户说"加入 requirements" 明确同意落地；worktree 命名遵循既有项目约定，不需要弹窗确认
