# 2026-06-18 16:50 — P0+P1 + 公开准备开工

## 用户原始 Prompt
- 上一轮决策：license=MIT（默认）, cookie=SweetCookieKit（推荐）, tcc=detect+UI引导（推荐）
- 用户目标：先完成 P0 + P1，并把项目公开给社区用，但属于个人项目

## 启动时分支 / 版本
- 主仓库：/Users/taobe/Projects/GitHub/Personal/quota-bar
- HEAD：43fe088 update code
- 工作分支：feat/real-data-and-public（新建）
- worktree 路径：.worktrees/feat-real-data-and-public
- 开始时间：2026-06-18 16:50 (UTC+8)
- 结束时间：进行中

## 已阅读上下文
- 上一轮的 CodexBar 对比调研报告（.worktrees/investigate-codex-bar/research/codex-bar-comparison.md）
- 当前根目录的所有 Swift 源文件（QuotaModels, BrowserCookieReader, BrowserCookieProvider, KeychainProvider, CLILogProvider, ProviderFactory, RefreshCoordinator, AgentDetector, AgentModel, AgentStore, PlaceholderProviders, MenuView, StatusBarController, CodingPlanMenuApp）
- Package.swift（无外部依赖）
- 当前 6 个 worktree 状态（main / sub-a / ref-a / investigate-codex-bar / ui-c / 新建 feat-real-data-and-public）

## 完成工作（本条日志截止时）

### 1. 把 SUB-A 未提交的散落代码带进新 worktree
- 主仓库 main 工作区有 11 个 SUB-A 文件未提交（AgentDetector, AgentModel, AgentStore, BrowserCookieProvider, BrowserCookieReader, CLILogProvider, KeychainProvider, PlaceholderProviders, ProviderFactory, QuotaModels, RefreshCoordinator）+ MenuView.swift 修改
- `git stash push -u -m ...` 抓取 → 新建 worktree `feat-real-data-and-public` → `git stash pop` 应用
- 发现：main HEAD 上的 StatusBarController 是"未接 RefreshCoordinator"的最小版本，SUB-A 之前的"完整接好"版本只活在 main 工作区里，没 commit 也没 stash

### 2. 修复 UI 编译错误
- `MenuView.swift`：补齐缺失的样式常量 `summarySpacing` / `sectionSpacing` / `quotaRowsSpacing` / `quotaRowSpacing` / `contentBlockSpacing`
- `StatusBarController.swift`：整体重写为"接 RefreshCoordinator + Combine 订阅 state/isRefreshing + 用 coordinator.state/isRefreshing/lastUpdatedText 渲染"的完整版本；删掉 deinit 里的 main-actor-isolated 调用（Swift 6 限制）
- `MenuDashboardStyle` 缺 `height` 常量 → 改用 `height: 1` + fittingSize 动态计算

### 3. 验证构建
- `swift build` → Build complete!（0.86s）
- `swift run` 启动 4s 不崩溃

## 下一步计划
- P0: 加 SweetCookieKit 依赖 + TCC 检测 + 真读 Keychain + 填 3 个 dashboard endpoint + parser
- P1: ProviderFetchStrategy 协议 + Pipeline + TTYCommandRunner + 把 AgentDetector 与 QuotaProvider 合一
- 公开准备: LICENSE(MIT) + README + CHANGELOG + GitHub Actions

## 验证方式
- `swift build` 通过
- `swift run` 启动后 4 秒存活
- 待续：完成 P0 后用真实 cookie / CLI / Keychain 数据回归

## 备注
- LICENSE 默认采用 MIT，若用户要求换 Apache 2.0 / BSD-3 跟我说一声
- main 工作区现在还残留一些 .build/ 编译产物修改（git status 里能看到），与本次工作无关