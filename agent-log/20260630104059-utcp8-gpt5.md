# 20260630104059 - sub/main：登记持久化与来源索引需求

## 用户原始 prompt

> 把这些需求纳入requirements

补充：

> 把这些需求纳入requirements

## 启动运行时的分支和版本

- 分支：`sub/main`
- 提交：`57694eff7fa09d299a2b59404d7b4ca1beea525d`
- 备注：按规范先执行 `git pull`，但远端拉取超过 15 秒未返回；用户重复要求继续登记需求，因此中断拉取并基于当前工作区执行。

## 任务开始时间

2026-06-30 10:36 (UTC+8)

## 任务结束时间

2026-06-30 10:40 (UTC+8)

## 任务结束时是否执行了提交

否。

## 已阅读上下文

- `REQUIREMENTS.md`
- `git status --short`
- 上一轮关于 Application Support、内存实时值、来源索引、last-known-good 快照与样例数据移除的对话

## 对话与行动记录

- 用户要求将已讨论的持久化策略纳入 `REQUIREMENTS.md`。
- 尝试按 `AGENTS.md` 执行 `git pull`，但命令长时间无返回；用户重复相同指令后中断拉取并继续。
- 查找 `REQUIREMENTS.md` 中已有 phase 与 DATA/PM/ARCH 相关任务，确认该需求属于用户可感知的数据持久化、刷新稳定性与 UI 语义。
- 在 `REQUIREMENTS.md` 末尾新增 `Phase - v0.9.0 - 持久化、来源索引与启动兜底`。

## 完成工作

- 新增 `v0.9.0` phase，覆盖：
  - Application Support 作为 Quota Bar 自有持久化目录；
  - schema version 与安全边界；
  - provider 来源索引缓存；
  - 浏览器 Cookie / CLI 配置来源索引；
  - last-known-good 快照；
  - 启动、更新、重装、刷新期间的 stale 兜底；
  - 刷新失败或不可用时不得用旧快照假装可用；
  - 移除主路径样例数据 provider。

## 更新的需求 ID

- 新增：
  - `0.9.0-ARCH-A-000` 至 `0.9.0-ARCH-A-002`
  - `0.9.0-DATA-A-000` 至 `0.9.0-DATA-A-003`
  - `0.9.0-DATA-B-000` 至 `0.9.0-DATA-B-005`
  - `0.9.0-UI-A-000` 至 `0.9.0-UI-A-002`

## 更新的 README 或 DESIGN 章节

无。

## 验证方式

- `rg -n "Phase - v0\\.9\\.0|0\\.9\\.0-" REQUIREMENTS.md`
- `git diff -- REQUIREMENTS.md`

## 备注

- 本次未改源码，未运行构建或测试。
