# 用户原始 prompt

> Kimi Work 没有恢复，而且你刚刚改到一半，有一瞬间是恢复了的，只是最后打包的版本又没有，你也可以参考附件 2 的模型日志之前的版本  
> 对齐也没有恢复  
> - 参考一下附件 2 的这份模型日志之前版本的样式  
> 整组拖动也做错了  
> - 对于像 video 日额度、video 周额度这样的对象，它们是一个整组，不需要单独的那个 video 那一行的标签  
>   
> 现在只有整个模型的拖动排序功能（比如kimi和codex之间拖动排序）以及动态刷新功能是对的

补充 prompt：

> 关于 Kimi Work 检查之前版本的代码，我们之前是实现过的，你不用自己创新

# 启动运行时的分支和版本

- 分支：`main`
- HEAD：`89e9d3ac611edb76c6130108425d1152c83fa893`
- 备注：本轮开始前工作区已有大量未提交改动，包含其它 agent/模型的文件修改与新文件；未回滚无关改动。
- `git pull`：此前同一任务链路中尝试过但网络/远端交互无结果，被中断；本轮基于当前本地 dirty workspace 修复和打包。

# 任务开始时间

2026-06-25 12:05 Asia/Shanghai

# 任务结束时间

2026-06-25 12:15 Asia/Shanghai

# 任务结束时是否执行了提交

否。

# 已阅读上下文

- `AGENTS.md`
- `REQUIREMENTS.md`
- `agent-log/20260625104200-utcp8-mavis.md`
- `agent-log/20260621014158-utcp8-gpt5.md`
- `agent-log/20260624163000-utcp8-gpt5.md`
- `quota-bar/Sources/QuotaBar/MenuView.swift`
- `quota-bar/Sources/QuotaBar/BrowserCookieProvider.swift`
- `quota-bar/Sources/QuotaBar/KimiAuthProvider.swift`
- `quota-bar/Sources/QuotaBar/DashboardEndpoints.swift`
- `quota-bar/Sources/QuotaBar/Strategies.swift`
- `quota-bar/Sources/QuotaBar/ProviderFetchStrategy.swift`

# 对话与行动记录

1. 核对用户截图与反馈，确认当前 regression 不是 provider 整体拖拽或动态刷新，而是 dropdown 多订阅组渲染、组拖拽可视表达、Kimi Work 数据路径。
2. 检查 `MenuView.SubscriptionGroupBlock`，发现仍然渲染 `SubscriptionGroupHeader`，导致 “Gemini / Other / General / Video” 额外标签行出现；多订阅组路径也未复用单组 quota rows 的左缩进，造成对齐错位。
3. 修改 `SubscriptionGroupBlock`：移除可见子组 header，不添加子组灯；保留 block 本身作为 drag/drop 作用域，并给内部 quota rows 补回 `leadingGlyphColumn` 缩进。
4. 根据用户补充要求回查旧日志，确认 Kimi Work 旧实现是浏览器 cookie 路径：`kimi-auth` cookie 调 `GetSubscriptionStat` 返回 Work + Code；`KimiAuthProvider` 的 CLI OAuth 路径曾验证无法调用该 membership endpoint，应该只作为 Code-only fallback。
5. 撤回本轮早先对 `KimiAuthProvider` 的 membership endpoint 尝试，恢复其只读取 CLI `coding/v1/usages`。
6. 修正 `BrowserCookieProvider.fetchKimiSnapshot`：额度请求 `GetSubscriptionStat` 是必需路径，先独立请求并解析；`GetSubscription` 只作为 tier/price 的可选补充，使用短超时，失败或超时不再阻塞 Work 额度显示。
7. 更新 `REQUIREMENTS.md`，记录子组不可见但整组拖拽、Kimi Work 已验证数据路径等语义，避免后续再误改。
8. 运行构建和打包，生成新的 app bundle。

# 完成工作

- `MenuView.swift`
  - 去掉多订阅组 provider 的可见子组标签行。
  - 不再显示子组状态灯。
  - 组内多条 quota 仍由同一个 `SubscriptionGroupBlock` 包裹，拖动时作为整组移动。
  - 多组 quota rows 恢复与单组 provider 一致的左侧缩进。
- `KimiAuthProvider.swift`
  - 撤回 CLI OAuth 调 `GetSubscriptionStat` 的尝试，恢复旧职责：只作为 Code-only fallback。
- `BrowserCookieProvider.swift`
  - Kimi cookie 路径先拉 `GetSubscriptionStat`，确保 Work/Code 三条额度不被可选 tier/price 请求阻塞。
  - `GetSubscription` 改为可选短超时补充，失败不影响额度。
- `REQUIREMENTS.md`
  - 更新 `0.3.0-UI-A-005` 说明。
  - 新增并完成 `0.3.0-UI-A-009`。
  - 新增并完成 `0.3.0-DATA-A-005`。

# 更新的需求 ID

- `[0.3.0-DATA-A-005]`
- `[0.3.0-UI-A-005]`
- `[0.3.0-UI-A-009]`

# 更新的 README 或 DESIGN 章节

无。

# 验证方式

- `cd quota-bar && swift build`
  - 通过。
  - 仍有既有 warning：`MiniMaxConfigProvider.swift` 的 `prefix` 未使用、`EdgeCookieReader.swift` 的 `placeholders` 未使用。
- `cd quota-bar && ./scripts/build-app.sh`
  - 通过。
  - 生成：`quota-bar/build/20260625-121436/QuotaBar.app`
  - `quota-bar/build/latest/QuotaBar.app` 已指向最新版本。

# 备注

- 未执行提交。
- 本轮没有回滚其它模型/用户造成的工作区改动。
- 若用户运行后 Kimi 仍只显示 Code，需要优先查看 Console 中 `QuotaBar: [kimi-cookie]` 日志，确认 Edge/Chrome cookie 路径是否成功读取到 `kimi-auth` 并请求 `GetSubscriptionStat`；不应再通过 CLI OAuth endpoint 创造 Work 数据。
