# 用户原始 prompt

```text
还是没有好：kimi有额度但没有work额度，没有正确的订阅名，但有正确的订阅费用；zcode没有好；看看附件的沟通有没有参考价值
```

附件：

- `/Users/taobe/Downloads/ChatGPT-macOS App 无密码授权.md`
- 截图显示：Kimi 只有 Code 5h / Code 周额度，显示 `Kimi Trial（已购）`；Z Code 显示 `BigModel Start` 但无 quota bar。

# 启动运行时的分支和版本

- 分支：`sub/main`
- 版本：`6fe1b70331b72b8ab57c372e7c2606cad7c80c2a`
- `git pull`：首次因 worktree `.git` 元数据写入受限失败；按权限流程执行后显示 `Already up to date.`

# 任务开始时间

2026-07-02 21:47:37 +08:00

# 任务结束时间

2026-07-02 21:56:20 +08:00

# 任务结束时是否执行了提交

否

# 已阅读上下文

- `AGENTS.md`
- `README.md`
- `REQUIREMENTS.md`
- `DESIGN.md`
- `agent-log/20260702170822-utcp8-gpt5.md`
- `/Users/taobe/Downloads/ChatGPT-macOS App 无密码授权.md`
- `quota-bar/Sources/QuotaBar/KimiDesktopTokenProvider.swift`
- `quota-bar/Sources/QuotaBar/DashboardEndpoints.swift`
- `quota-bar/Sources/QuotaBar/KimiAuthProvider.swift`
- `quota-bar/Sources/QuotaBar/ZCodeAuthProvider.swift`
- `quota-bar/Sources/QuotaBar/QuotaModels.swift`
- `quota-bar/Tests/QuotaBarTests/KimiDesktopTokenProviderTests.swift`
- `quota-bar/Tests/QuotaBarTests/ZCodeAuthProviderTests.swift`
- 本地 Kimi Desktop token-store 和 Z Code 配置 / 日志，仅检查字段结构和 endpoint 线索，不输出 token / API key。

# 对话与行动记录

- 读取附件后确认其参考价值：默认主链路不应回到浏览器 Cookie；长期方向应是 provider connector、App-owned WebView 登录、浏览器扩展或网页自动化兜底。本次先按现有架构修 Kimi / Z Code 的本地 API 路径。
- Kimi 问题定位：`KimiDesktopTokenProvider` 只调用 `GetSubscriptionStat`，而订阅名/价格来自 `GetSubscription`；同时 Work parser 只识别 `subscriptionBalance.amountUsedRatio`，schema 变化时会只剩 Code。
- 本地 token-store 结构确认：`msh_user_subscription_data` 只有 `currentMembershipLevel: 15`，可作为最后兜底；不应替代 `GetSubscription`。
- Z Code 问题定位：当前代码把 `zcode-plan/anthropic` 错推到 `/api/monitor/usage/quota/limit`；Z Code 本地日志显示真实接口为 `https://zcode.z.ai/api/v1/zcode-plan/billing/balance?app_version=3.1.5`，响应含 `balances[]`、`total_units`、`remaining_units`、`period_end`。

# 完成工作

- `KimiDesktopTokenProvider` 改为复合请求：
  - 必需：`GetSubscriptionStat` 获取 Work + Code + 到期日；
  - 可选：`GetSubscription` 获取 Andante/Moderato 等订阅名和价格；
  - 请求增加 `Cookie: kimi-auth=<token>`、`x-msh-platform`、`connect-protocol-version` 等 Web 端 header，降低 Desktop token 被服务端区别对待的概率；
  - 本地 `currentMembershipLevel=15` 只作为最后兜底映射到 Andante。
- `KimiSubscriptionStatParser` 增强 Work 解析：
  - 支持 `amountUsedRatio`；
  - 支持 `used/total`；
  - 支持 `remaining/total`。
- `ZCodeAuthProvider` 改为优先解析 `zcode-plan/billing/balance`：
  - 从 `~/.zcode/v2/config.json` 的 `https://zcode.z.ai/api/v1/zcode-plan/anthropic` 推导到 `billing/balance`；
  - 解析 GLM-5.2 / GLM-5-Turbo 的 daily token quota；
  - 保留旧 `api/monitor/usage/quota/limit` parser 作为兼容 fallback。
- `ProviderPricing.normalizedTier` 增加 `zcode-v3-start-plan-*` / `zcode-v3-coding-plan-*` 的可读名称映射。
- Kimi 安装探测加入 Desktop token-store 路径。
- 更新 `README.md` 和 `REQUIREMENTS.md`。
- 打包新版 app：`quota-bar/build/20260702-215511/QuotaBar.app`，`build/latest` 已指向该版本。

# 更新的需求 ID

- `0.2.0-DATA-B-026`
- `0.4.0-DATA-A-007`

# 更新的 README 或 DESIGN 章节

- `README.md`：功能 / 四层获取矩阵
- `DESIGN.md`：未更新

# 验证方式

- `cd quota-bar && env CLANG_MODULE_CACHE_PATH=.build/clang-module-cache SWIFT_MODULE_CACHE_PATH=.build/swift-module-cache swift test --disable-sandbox`
  - 结果：60 tests in 15 suites passed。
- `cd quota-bar && ./scripts/build-app.sh`
  - 结果：生成 `build/20260702-215511/QuotaBar.app`。
- `cd quota-bar && codesign --verify --deep --verbose=2 build/latest/QuotaBar.app`
  - 结果：valid on disk，satisfies its Designated Requirement。

# 备注

- 本次未提交。
- 本次没有打印或保存任何 token / API key 原文。
- Z Code 的 live API 请求未在 agent 环境中直接发出；依据来自本机 Z Code 日志中的成功请求和响应结构，并用单元测试覆盖 parser。
