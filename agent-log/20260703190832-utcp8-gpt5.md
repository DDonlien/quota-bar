# 用户原始 prompt

> 很奇怪的是，菜单栏里有agy的额度，但是下拉里显示没有，我不知道是哪里错了？
> web view的功能似乎也还没实现，我的想法是下拉里支持webview的如果没获取到，提供一个可点击的文本引导用户打开webview浏览器授权

# 启动运行时的分支和版本

- 分支：`sub/main`
- 提交：`c3f11a613c0442a236b160ac563b1e87313ff42f`
- `git pull`：首次受沙箱限制失败；经授权后执行，结果 `Already up to date.`

# 任务开始时间

2026-07-03 19:03:00 +0800

# 任务结束时间

2026-07-03 19:08:32 +0800

# 任务结束时是否执行了提交

未提交。

# 已阅读上下文

- `AGENTS.md`
- `README.md`
- `REQUIREMENTS.md`
- `DESIGN.md`
- `agent-log/20260702223030-utcp8-gpt5.md`

# 对话与行动记录

- 用户指出菜单栏显示 Antigravity 似乎有额度，但下拉显示待配置；同时提出 WebView 授权入口应在下拉中以可点击文本引导。
- 检查 `StatusBarController.drawableSnapshots` 后确认根因：`.needsConfiguration` 被当作 drawable snapshot 绘制为 50% bar，因此菜单栏不是显示真实 AGY 额度，而是误画了待配置占位。
- 修改菜单栏绘制规则：只绘制 `.available`（且有 quota）、`.loading`、`.subscriptionExpired`；不再绘制 `.needsConfiguration`。
- 新增 `WebAuthorizationController`：为支持网页授权的 provider 打开 app 内 WKWebView 窗口，使用默认持久 `WKWebsiteDataStore` 保存登录态。
- 在下拉待配置状态中为支持 WebView 的 provider 显示「打开 WebView 授权」可点击文本。

# 完成工作

- 修复 AGY 菜单栏/下拉状态不一致的误导性展示。
- 新增 WebView 授权容器基础能力，支持 Codex/OpenAI、Claude、Kimi、MiniMax、Antigravity、Z Code 的登录页入口；Antigravity 后续需标为备用入口，不作为主额度链路。
- 新增 `StatusBarControllerTests` 覆盖 `needsConfiguration` 不绘制菜单栏占位 bar。
- 更新 `REQUIREMENTS.md`：
  - `0.2.0-FE-A-010`
  - `0.9.0-DATA-A-006`
- 打包新版本：`quota-bar/build/20260703-190820/QuotaBar.app`。

# 更新的需求 ID

- `0.2.0-FE-A-010`
- `0.9.0-DATA-A-006`

# 更新的 README 或 DESIGN 章节

无。

# 验证方式

- `cd quota-bar && env CLANG_MODULE_CACHE_PATH=.build/clang-module-cache SWIFT_MODULE_CACHE_PATH=.build/swift-module-cache swift test --disable-sandbox`
  - 结果：通过，64 个 Swift Testing 测试通过。
- `cd quota-bar && ./scripts/build-app.sh`
  - 结果：通过，生成 `/Users/taobe/Projects/GitHub/Personal/quota-bar/worktree/sub-main/quota-bar/build/20260703-190820/QuotaBar.app`。

# 备注

- 当前 WebView 只完成“用户可点击打开并持久登录”的授权容器；各 provider 后续还需要新增 Web dashboard strategy，才能用该 WebView session 隐式请求额度/过期日。
