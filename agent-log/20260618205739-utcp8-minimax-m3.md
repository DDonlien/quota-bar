# 任务执行日志：把 CFBundleDisplayName 改为 "Quota Bar"

## 用户原始 prompt

> 你来改

接续上一轮对话：用户对「菜单栏应用在系统设置里到底叫什么名字」提出疑问（因为 `CodingPlanMenu` 和 Quota Bar 是同一个东西，担心找错应用、给错权限）。我提议修改 [build-app.sh](file:///Users/taobe/Projects/GitHub/Personal/quota-bar/drop-down-test/CodingPlanMenu/build-app.sh) 的 `CFBundleDisplayName` 为 "Quota Bar"，用户回复「你来改」。

## 启动时状态

- 分支：主 worktree 在 `main`（HEAD = 158fcd2，领先 origin/main 5 commits，未 push）
- 时间：2026-06-18 20:55:00 +0800（启动）/ 20:57:39 +0800（完成）
- 是否提交：未提交（仅修改 build-app.sh 的一行字符串，未运行 git add）

## 已阅读上下文

- [build-app.sh](file:///Users/taobe/Projects/GitHub/Personal/quota-bar/drop-down-test/CodingPlanMenu/build-app.sh)（全部 57 行）
- 上一条日志 [20260618174200-utcp8-minimax-m3.md](file:///Users/taobe/Projects/GitHub/Personal/quota-bar/agent-log/20260618174200-utcp8-minimax-m3.md)（worktree 合并）
- [REQUIREMENTS.md](file:///Users/taobe/Projects/GitHub/Personal/quota-bar/REQUIREMENTS.md)：本次修改不涉及需求勾选状态变化
- [AGENTS.md](file:///Users/taobe/Projects/GitHub/Personal/quota-bar/AGENTS.md) 关于行为/共享逻辑变更需更新测试的规则

## 完成工作

### 1. 定位

[build-app.sh](file:///Users/taobe/Projects/GitHub/Personal/quota-bar/drop-down-test/CodingPlanMenu/build-app.sh) 的第 34-35 行：

```xml
<key>CFBundleDisplayName</key>
<string>CodingPlanMenu</string>
```

### 2. 单点编辑

```diff
- <string>CodingPlanMenu</string>
+ <string>Quota Bar</string>
```

只改 `CFBundleDisplayName` 的值，保持其余字段不动：

- `CFBundleIdentifier` = `com.example.CodingPlanMenu`（保持不变）
- `CFBundleName` = `CodingPlanMenu`（保持不变）
- `CFBundleExecutable` = `CodingPlanMenu`（保持不变）
- `APP_NAME` = `CodingPlanMenu`（保持不变 → `.app` 文件夹名仍叫 `CodingPlanMenu.app`，二进制仍叫 `CodingPlanMenu`）

### 3. 设计权衡

只改 `CFBundleDisplayName`，最小化影响：

- ✅ 系统设置、Keychain 授权弹窗（"Quota Bar 需要 Keychain 授权"）、Dock / 启动台 → 用户可见名称统一为 "Quota Bar"
- ✅ 与 [CodingPlanMenuApp.swift](file:///Users/taobe/Projects/GitHub/Personal/quota-bar/drop-down-test/CodingPlanMenu/Sources/CodingPlanMenu/CodingPlanMenuApp.swift) 中的 `BrowserCookieKeychainPromptHandler` 文案口径一致
- ✅ 不需要重命名二进制、重打包路径、改 bundle id；TCC 已有的授权记录仍然有效（按 bundle id 匹配）
- ⚠️ Finder / 终端中 `.app` 文件夹仍是 `CodingPlanMenu.app`（这是 `APP_NAME` 决定的，本次未改）
- ⚠️ `CFBundleName`（菜单栏 app 通常无 dock name，但 Launchpad/某些系统面板会用）仍是 `CodingPlanMenu`

如用户后续希望 Finder 文件夹也改名，需要单独改 `APP_NAME`；但那会触发新的 bundle id 路径变化与 TCC 重新授权，故本次不动。

## 验证方式

- 静态：cat 已确认第 35 行值为 `Quota Bar`；第 31 / 33 / 37 行 bundle id / bundle name / bundle executable 仍是 `CodingPlanMenu` / `com.example.CodingPlanMenu`
- 构建：未运行（沙箱限制同上一条日志）；重新运行 `./build-app.sh` 后生成的 `Info.plist` 将包含 `Quota Bar`
- 应用行为：未启动应用验证（用户在系统设置里看到的应用名才是验证点）

## 需求 / 设计更新

- `REQUIREMENTS.md`：未变更
- `README.md`：未变更
- `DESIGN.md`：未变更
- `AGENTS.md`：未变更

## 备注

- 本次修改未提交，等用户决定是否一并把 `build-app.sh` 与上一轮的 padding 改动一起 commit
- Keychain 授权提示：「Quota Bar 需要 Keychain 授权」已经与新 DisplayName 一致，不会出现名字错位
- 用户在系统设置 → 隐私与安全 → 完全磁盘访问权限 中，应能找到「Quota Bar」应用并勾选；如果之前已经按 `CodingPlanMenu` 授权过，按 bundle id 仍会匹配，状态保持不变
- 之前给用户的引导语（"找 Quota Bar 应用授权"）从这一版打包后开始真正生效
