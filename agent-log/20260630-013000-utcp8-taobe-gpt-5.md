# 20260630-013000 - 打包应用名与图标更新

## 用户原始 prompt

> 把打出来的应用名称中间加上空格，并且图标换成我们最新的图标

## 启动运行时的分支和版本

- 分支：`main`
- 提交：`1532061d80cab44eba9d5469836105dedd72a7e8`
- 任务开始状态：`main...origin/main [ahead 1]`，且存在未提交改动：`README.md`、`REQUIREMENTS.md`、`macos/scripts/build-app.sh`、`agent-log/20260630-004605-utcp8-taobe-gpt-5.md`

## 任务开始时间

2026-06-30 01:02:00 CST (+0800)

## 任务结束时间

2026-06-30 01:30:00 CST (+0800)

## 任务结束时是否执行了提交

否。

## 已阅读上下文

- 根目录 `AGENTS.md`
- 根目录 `README.md`
- 根目录 `REQUIREMENTS.md`
- 根目录 `DESIGN.md`
- 最新执行日志 `agent-log/20260630-004605-utcp8-taobe-gpt-5.md`
- `macos/scripts/build-app.sh`
- `.github/workflows/release.yml`
- `Makefile`
- `site/public/logo-512-roundcorner.png` 与 `reference/design/logo-512-roundcorner.png` 图标资源信息

## 对话与行动记录

- 按协作规范执行 `git fetch --prune`，远端追踪信息更新完成；当前分支未落后远端。
- 确认脚本中 `CFBundleDisplayName` 已是 `Quota Bar`，但 `.app` bundle 目录名和 `CFBundleName` 仍是 `QuotaBar`。
- 选用仓库中更新时间更新、面向站点公开展示的 `site/public/logo-512-roundcorner.png` 作为最新图标来源，并生成 `macos/Resources/QuotaBar.icns`。
- 修改 `macos/scripts/build-app.sh`：区分可执行文件名 `QuotaBar` 和用户可见应用名 `Quota Bar`；打包产物改为 `Quota Bar.app`；复制 `QuotaBar.icns` 到 bundle；写入 `CFBundleIconFile`；修正 `CFBundleVersion` plist 标签；将 codesign 移到 `Info.plist` 写入之后。
- 修改 `.github/workflows/release.yml`：DMG 打包阶段改为查找 `Quota Bar.app`，DMG 文件名仍保留 `QuotaBar-<sha>.dmg`。
- 修改 `README.md` 与 `Makefile` 中的打包产物路径说明。
- 修改 `REQUIREMENTS.md`：新增并完成 `0.5.0-ENG-A-005`。
- 执行 `make app` 真实打包。首次沙箱内失败于 Swift 用户级缓存权限；提升权限后失败于旧 ModuleCache 记录了 `worktree/main` 路径；清理派生 ModuleCache 后打包成功。
- 成功打包后 `macos/build/latest` 相对软链更新为 `20260630-012901`。

## 完成工作

- 本地打包产物名称已变为 `Quota Bar.app`。
- Bundle 内已包含新版圆角应用图标 `Contents/Resources/QuotaBar.icns`，`Info.plist` 已声明 `CFBundleIconFile = QuotaBar`。
- CI Release 的 DMG 打包路径已同步支持带空格的 app bundle 名。
- README / Makefile / REQUIREMENTS 已同步更新。

## 更新的需求 ID

- 新增并完成 `0.5.0-ENG-A-005`。
- 保留上一任务已新增的 `0.5.0-ENG-A-004`。

## 更新的 README 或 DESIGN 章节

- `README.md`：打包成 `.app` 章节中的产物示例和 latest 路径改为 `Quota Bar.app`。
- `DESIGN.md`：无更新。

## 验证方式

- `bash -n macos/scripts/build-app.sh` 通过。
- `file macos/Resources/QuotaBar.icns` 确认为 Mac OS X icon。
- `make app` 通过，生成 `macos/build/20260630-012901/Quota Bar.app`。
- `readlink macos/build/latest` 返回 `20260630-012901`。
- `test -d 'macos/build/latest/Quota Bar.app'` 通过。
- `test -f 'macos/build/latest/Quota Bar.app/Contents/Resources/QuotaBar.icns'` 通过。
- `plutil -lint 'macos/build/latest/Quota Bar.app/Contents/Info.plist'` 通过。
- `/usr/libexec/PlistBuddy` 确认 `CFBundleName = Quota Bar`、`CFBundleDisplayName = Quota Bar`、`CFBundleIconFile = QuotaBar`。
- `codesign --verify --deep --strict --verbose=2 'macos/build/latest/Quota Bar.app'` 通过。

## 备注

- 本次没有提交。
- `macos/build/latest` 是实际打包验证导致的软链目标更新。
