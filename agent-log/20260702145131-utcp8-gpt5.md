# 用户原始 prompt

> 光codex不够，我需要一个：
> 首先设计一套完整链路
> 然后针对每个模型，设计：哪些节点可以跳过；每个节点需要如何特殊处理（比如有的cli是/usage，有的是/quota，如果用浏览器，每个网址不一样）

# 启动运行时的分支和版本

- 分支：`sub/main`
- 提交：`bc1ee47d8ad7a853618a539e0cf4ea9613fd7ec8`
- 已执行：`git pull`，结果为 already up to date。

# 任务开始时间

2026-07-02 14:41 Asia/Shanghai

# 任务结束时间

2026-07-02 14:51 Asia/Shanghai

# 任务结束时是否执行了提交

未提交。

# 已阅读上下文

- `AGENTS.md`
- `README.md`
- `REQUIREMENTS.md`
- `DESIGN.md`
- `agent-log/20260702143521-utcp8-gpt5.md`
- `quota-bar/Sources/QuotaBar/Strategies.swift`
- `quota-bar/Sources/QuotaBar/QuotaModels.swift`
- `quota-bar/Sources/QuotaBar/ProviderFetchStrategy.swift`

# 对话与行动记录

- 用户明确要求不要只设计 Codex 单点修复，而是设计一套完整获取链路，并针对每个 provider/model 标明哪些节点可以跳过、哪些节点需要特殊处理。
- 检查当前实现，确认现有 `FetchPipeline` 已有 strategy/fallback 能力，但缺少统一的 source kind、provider capability matrix、权限/交互成本和跳过规则。
- 在 `REQUIREMENTS.md` 的 v0.9.0 中新增 `ARCH-B：统一额度获取链路与 Provider 能力矩阵`，登记统一链路、能力矩阵、CLI/RPC 特殊命令、Browser dashboard 特殊 URL、权限策略和订阅状态归一化要求。

# 完成工作

- 更新 `REQUIREMENTS.md`，新增需求 ID：
  - `0.9.0-ARCH-B-000`
  - `0.9.0-ARCH-B-001`
  - `0.9.0-ARCH-B-002`
  - `0.9.0-ARCH-B-003`
  - `0.9.0-ARCH-B-004`
  - `0.9.0-ARCH-B-005`
  - `0.9.0-ARCH-B-006`
- 形成统一获取链路和 provider 能力矩阵设计，将在回复中交付。

# 更新的需求 ID

- `0.9.0-ARCH-B-000` 至 `0.9.0-ARCH-B-006`

# 更新的 README 或 DESIGN 章节

无。

# 验证方式

- 文档变更，无构建验证。
- 使用 `rg` / `sed` 检查现有 pipeline、provider metadata 和需求上下文。

# 备注

- 本次未改 Swift 实现代码。
- 工作区仍存在未跟踪日志文件，未进行 git add/commit。
