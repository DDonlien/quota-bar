# 20260629-203001 - 仓库结构适配新 agent-template

## 用户原始 prompt

> 参考新的/Users/taobe/Projects/GitHub/Personal/quota-bar/agent-template，更新当前仓库的文件夹结构

追加要求：

> 并修改agents.md和requirements.md的内容and/or结构以适配新的template规则

## 启动运行时的分支和版本

- 分支：`main`
- Git 同步：按任务开始时根 `AGENTS.md` 旧规则执行 `git pull`，结果 `Already up to date.`
- 提交：`deee18e`
- 工作区：任务开始前已有 `agent-template/AGENTS.md`、`agent-template/REQUIREMENTS.md`、模板日志和 `web/reference/` 等未提交改动；本次未回滚这些既有改动。

## 任务开始时间

2026-06-29 20:16:00 CST (+0800)

## 任务结束时间

2026-06-29 20:30:01 CST (+0800)

## 任务结束时是否执行了提交

否。

## 已阅读上下文

- 根目录 `AGENTS.md`、`README.md`、`REQUIREMENTS.md`、`DESIGN.md`
- `agent-template/AGENTS.md`、`agent-template/README.md`、`agent-template/REQUIREMENTS.md`、`agent-template/DESIGN.md`
- 最近 3 条根执行日志：`20260627224800-utcp8-mavis.md`、`20260627162000-utcp8-mavis.md`、`20260626160800-utcp8-mavis.md`
- `site/` 子项目文档（由原 `web/` 迁移而来）
- `.github/workflows/`、`Makefile`、`vercel.json`、`.gitignore`

## 对话与行动记录

- 对比新模板后，识别新增规则：顶层目录按交付物 / 职责命名；macOS 原生应用用 `macos/`；官网/落地页用 `site/`；Git worktree 工作目录推荐 `worktrees/`；模板目录需清理嵌套 `.git` 和模板历史日志。
- 将应用目录 `quota-bar/` 迁移为 `macos/`，站点目录 `web/` 迁移为 `site/`。
- 使用 `git worktree move` 将已注册 worktree 从 `.worktrees/` 迁移到 `worktrees/`，保持 `.git/worktrees` 元数据一致。
- 清理 `agent-template/.git` 和模板历史日志，仅保留 `agent-template/agent-log/yyyymmdd-hhmmss-utcpn-username-modelname.md`。
- 用新模板标准内容重建根 `AGENTS.md`，并更新项目专用内容中的目录索引、命令入口、子功能入口、安全隐私要求。
- 更新 `REQUIREMENTS.md` 和 `site/REQUIREMENTS.md` 的三级标题结构为 `branch-name: feature description`，保留既有稳定 ID。
- 更新 README、DESIGN、Makefile、Vercel、GitHub Actions、site 子项目文档中的目录路径。
- 修复移动目录后 `macos/build/latest` 仍指向旧绝对路径的问题，改为相对链接。
- 将 site 包名从 `quota-bar-web` 更新为 `quota-bar-site`。

## 完成工作

- 顶层结构迁移为：`macos/`、`site/`、`worktrees/`。
- 根 `AGENTS.md` 已适配新模板规则。
- 根 `REQUIREMENTS.md` 新增 v0.8.0 phase 记录本次结构迁移，并完成勾选。
- `site/REQUIREMENTS.md` 同步迁移为新模板三级标题结构。
- CI、Release、Vercel、Makefile 入口均指向新目录。

## 更新的需求 ID

- 新增并完成：`0.8.0-DOC-A-000` 至 `0.8.0-DOC-A-005`

## 更新的 README 或 DESIGN 章节

- `README.md`：快速开始、站点运行、打包、目录结构、构建命令路径更新。
- `DESIGN.md`：手动调参入口从旧原型路径更新为 `macos/Sources/QuotaBar/MenuView.swift`。
- `site/README.md`、`site/DESIGN.md`、`site/AGENTS.md`：从 `web/` 口径更新为 `site/`。

## 验证方式

- `git worktree list --porcelain`：所有附加 worktree 路径均在 `worktrees/` 下。
- `find agent-template ...`：未发现嵌套 `.git`，仅保留新日志模板文件。
- `make build`：通过（首次因旧 `.build` 缓存仍记录 `quota-bar/` 路径失败；清理 `macos/.build` 后通过）。
- `make test`：通过，20 tests in 4 suites passed。
- `make site`：通过，Astro build 输出到 `site/dist/`。
- 路径扫描：除新模板规则示例和迁移需求说明外，未发现旧 `web/`、`.worktrees/`、`drop-down-test` 等可执行路径引用。

## 备注

- `agent-template/AGENTS.md` 与 `agent-template/REQUIREMENTS.md` 在任务开始前已经是 modified 状态，本次没有回滚这些用户既有改动。
- 目录迁移尚未提交；`git status` 会把 `quota-bar/` 到 `macos/`、`web/` 到 `site/` 展示为删除 + 新增，提交时 Git 通常会识别为 rename。
