# 2026-06-30 订阅过期日数据源层级确认

## 用户原始 prompt

> 过期日我们是否还是按照：
> - app缓存（比如.codex或者内存信息，我不知道，额度的时候用过）
> - cli指令
> - web信息抓取
> 这三层在做？额度是这样

## 启动运行时的分支和版本

- 分支：`sub/expiry`
- `git pull`：Already up to date
- HEAD：`14945f6fc0973cba3d2b60a342472ca77284c626`
- 状态：开始时已有未提交日志 `agent-log/20260630123525-utcp8-gpt5.md`

## 任务开始时间

2026-06-30 12:42 (UTC+8)

## 任务结束时间

2026-06-30 12:44 (UTC+8)

## 任务结束时是否执行了提交

否

## 已阅读上下文

- `quota-bar/Sources/QuotaBar/Strategies.swift`
- `quota-bar/Sources/QuotaBar/RefreshCoordinator.swift`
- `quota-bar/Sources/QuotaBar/CodexAuthProvider.swift`
- `quota-bar/Sources/QuotaBar/KimiAuthProvider.swift`
- `quota-bar/Sources/QuotaBar/BrowserCookieProvider.swift`
- `quota-bar/Sources/QuotaBar/Harvesters.swift`

## 对话与行动记录

1. 确认额度 pipeline 仍按 provider 有序 fallback：本地 auth/config/CLI、浏览器 Cookie、Keychain 等。
2. 确认订阅过期日不是独立复制额度的三层 pipeline，而是在 quota snapshot 成功后补充。
3. Kimi 例外：Cookie 路径和 CLI OAuth 路径都尝试通过 `GetSubscriptionStat` / `subscriptionBalance.expireTime` 直接拿 `subscriptionExpiresAt`。
4. Codex / Claude / Cursor / MiniMax / Antigravity：如果额度 snapshot 可用且自身没有日期，则 `RefreshCoordinator.enrichWithHarvester` 用 `WKWebViewHeadlessLoader` 抓订阅页补日期。

## 完成工作

- 明确当前实现层级：额度仍是多层 fallback；过期日目前是「API 直接字段优先，否则 web/headless 订阅页补充」，没有独立 CLI 指令层。

## 更新的需求 ID

- 无。

## 更新的 README 或 DESIGN 章节

- 无。

## 验证方式

- 代码阅读确认，未运行测试。

## 备注

- 这意味着 Codex 的 `~/.codex/auth.json` 只用于额度 wham/usage；过期日仍需 headless 访问 ChatGPT 订阅管理页。
