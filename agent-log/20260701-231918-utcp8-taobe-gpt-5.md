# 用户原始 prompt

> 现在把 site/main merge 进来

# 启动运行时的分支和版本

- 分支：`main`
- 版本：`bf465ab39bf5db97762c853e529a230e279d2f20`
- 同步预检：`GIT_TERMINAL_PROMPT=0 git fetch --prune` 通过；`main` 与 `origin/main` 对齐，`site/main` 与 `origin/site/main` 对齐。

# 任务开始时间

2026-07-01 23:19:18 +0800

# 任务结束时间

2026-07-01 23:20:02 +0800

# 任务结束时是否执行了提交

是，提交将在日志更新后一并执行。

# 已阅读上下文

- 根目录 `AGENTS.md`
- `site/AGENTS.md`
- `site/REQUIREMENTS.md`
- `site/main` 最新提交日志
- `site/main` 近期执行日志：
  - `site/agent-log/20260701-175800-utcp8-taobe-minimax-m3.md`
  - `site/agent-log/20260701-150000-utcp8-taobe-minimax-m3.md`
  - `site/agent-log/20260630-234500-utcp8-taobe-minimax-m3.md`
  - `agent-log/20260701-005000-utcp8-taobe-minimax-m3.md`
- `main...site/main` 差异统计与分叉后文件列表。

# 对话与行动记录

- 用户要求按新的合并规则把 `site/main` merge 回 `main`。
- 先检查 `main`、`site/main` worktree 与远端同步状态；初次 `git fetch --prune` 因 HTTPS 进程无输出挂起，改用 `GIT_TERMINAL_PROMPT=0 git fetch --prune` 成功。
- 检查差异确认：`site/main` 改动集中在 `site/`、站点日志、站点 public 静态资源与 `agent-log/branches/feat-website.md`，不涉及 macOS 应用代码。
- 执行 `git merge --no-ff --no-commit site/main`，保留真实 merge 拓扑；自动合并成功，无冲突。

# 完成工作

- 使用真实 `git merge --no-ff --no-commit site/main` 将 `site/main` 合入 `main`，保留 Git Graph 拓扑连线。
- 合并 `site/main` 的营销主页大版本升级：
  - 主页改为 Hero / ProductPreview / SupportedServices / Features / Pricing / FAQ / Footer 结构。
  - 新增中英双语文案字典、LocaleSwitcher 和客户端 i18n apply 脚本。
  - ProductPreview 使用 3 个真实 mp4 录屏：preview / detail / reorder。
  - 新增 MapleMonoNL 与 JetBrainsMono 字体文件。
  - 移除旧的 `MenuBarMockup`、`ProviderGrid`、`Showcase` 组件。
  - 合并 site 子项目与根目录的相关执行日志。
- 合并过程中没有冲突，也没有引入 macOS 应用代码改动。

# 更新的需求 ID

- `site/REQUIREMENTS.md` 中 `[0.2.0-FE-A-002]` 中英双语切换从 deferred 路线被实际实现推进；本次保留 `site/main` 的文档状态，不在 merge 中重写站点需求历史。

# 更新的 README 或 DESIGN 章节

- `site/README.md` 随 `site/main` 合并更新，记录当前站点结构和运行说明。
- `site/DESIGN.md` 未在本次 merge 中修改。

# 验证方式

- `cd site && npm run build`：通过，1 page built in 406ms。
- `git diff --check`：通过。
- `git diff --cached --check`：通过。

# 备注

- 本次使用真实 Git merge，不再用普通功能移植提交替代。
