# 用户原始 prompt

只有minimax和agy是对的，kimi和zcode获取不到，还是在问我要浏览器权限（弹窗要了密码），codex有额度但额度不对（都是100%，明明之前还是对的）

补充：ok kimi似乎只是慢

# 启动运行时的分支和版本

- 分支：`sub/main`
- 提交：`afc75b15e01a253d5b997ac9b84dcb404c9182c4`
- `git pull`：沙盒内因 worktree `FETCH_HEAD` 权限失败，提权后显示 `Already up to date.`

# 任务开始时间

2026-07-02 16:25:00 UTC+8

# 任务结束时间

2026-07-02 16:35:09 UTC+8

# 任务结束时是否执行了提交

否。

# 已阅读上下文

- `AGENTS.md`
- `README.md`
- `REQUIREMENTS.md`
- `quota-bar/Sources/QuotaBar/ProviderFetchStrategy.swift`
- `quota-bar/Sources/QuotaBar/RefreshCoordinator.swift`
- `quota-bar/Sources/QuotaBar/Strategies.swift`
- `quota-bar/Sources/QuotaBar/CodexAuthProvider.swift`
- `quota-bar/Sources/QuotaBar/CLILogProvider.swift`
- `quota-bar/Sources/QuotaBar/KimiAuthProvider.swift`
- `quota-bar/Sources/QuotaBar/ZCodeAuthProvider.swift`
- `quota-bar/Sources/QuotaBar/QuotaPersistence.swift`
- `~/Library/Application Support/QuotaBar/provider-sources.json`
- `~/Library/Application Support/QuotaBar/snapshots.json`

# 对话与行动记录

- 根据用户实测截图确认问题集中在：默认刷新触发浏览器权限弹窗、Codex 退到 100% 本地日志估算、Z Code 实际失败原因被安装文案覆盖、Kimi 初始看似失败但用户补充为慢。
- 在 `REQUIREMENTS.md` 登记默认禁用浏览器 Cookie、Codex 真实 usage 优先、Kimi 本地 OAuth 优先、Z Code 保留真实失败原因等需求。
- 修改 `CodexAuthProvider`：`id_token` 过期/免费状态不再在请求 `wham/usage` 前短路；只有真实请求失败或 free 响应无法解析时才作为 expired/notSubscribed 辅助状态。
- 修改 `Strategies`：默认 pipeline 不再加入浏览器 Cookie strategy；Kimi 默认先走本地 OAuth；Codex 默认不再加入 `CLILogProvider`，避免本地日志估算写出 100%。
- 修改 `RefreshCoordinator`：pipeline 失败时保留真实错误原因，并追加安装探测原因，不再只显示“App 已装”。
- 修改 `QuotaPersistence`：启动读取缓存时丢弃 Codex `codex-cli` / localLog 估算快照，避免旧 100% 快照继续显示。
- 更新 README，说明 Full Disk Access 不再是默认要求，浏览器 Cookie 路径仅显式启用。
- 重写 Codex 相关测试并补充缓存过滤测试。
- 重新打包测试包。

# 完成工作

- 新测试包：`quota-bar/build/20260702-163447/QuotaBar.app`
- `quota-bar/build/latest` 已指向该测试包。
- 默认自动刷新不会主动读取浏览器 Cookie。
- Codex 不再因陈旧 `auth.json` 的过期日提前跳过真实 usage 请求，也不会默认用本地日志估算冒充真实额度。
- Z Code UI 将显示 API key/plan/endpoint 的真实失败原因，而不是只显示 App 安装状态。

# 更新的需求 ID

- `[0.2.0-DATA-B-022]`
- `[0.2.0-DATA-B-023]`
- `[0.2.0-DATA-B-024]`
- `[0.4.0-DATA-A-005]`

# 更新的 README 或 DESIGN 章节

- `README.md`：功能、TCC 引导、要求、快速开始、TCC / Full Disk Access 章节。

# 验证方式

- `swift test`
- `./scripts/build-app.sh`
- `plutil -p build/latest/QuotaBar.app/Contents/Info.plist`
- `codesign --verify --deep --verbose=2 build/latest/QuotaBar.app`
- `codesign -dv build/latest/QuotaBar.app`

# 备注

- 本次未提交 git commit。
- 未清理用户本机 `Application Support/QuotaBar` 文件；代码会在读取阶段过滤不可信的 Codex 本地日志估算缓存。
