# 用户原始 prompt

> 通用里增加语言选项，中文和英文
> 菜单栏的图标，不要一次性显示两个信息，合并就显示合并的描述，拆分就显示拆分的就好，也不用“合并：”
> 未激活的状态不应该能够移除激活
> provider需要跟随这个顺序展示内容：
> 名称（比如codex）
> 供应商（比如OpenAI）| 访问模式（App, CLI, Web，支持多少显示多少)
> Agy你somehow获取不到了

补充：

> 访问模式还有API，虽然现在其实不支持

# 启动运行时的分支和版本

- 分支：`preferences/main`
- 提交：`358a2fb`
- 备注：延续偏好设置 UI 调整任务，当前工作区已有未提交改动；本轮未执行 `git pull`。

# 任务开始时间

- 2026-07-01 14:25:00 +0800

# 任务结束时间

- 2026-07-01 14:34:09 +0800

# 任务结束时是否执行了提交

- 否。

# 已阅读上下文

- `quota-bar/Sources/QuotaBar/Preferences/GeneralSettingsView.swift`
- `quota-bar/Sources/QuotaBar/Preferences/ModelsSettingsView.swift`
- `quota-bar/Sources/QuotaBar/Preferences/ActivationSettingsView.swift`
- `quota-bar/Sources/QuotaBar/PreferencesStore.swift`
- `quota-bar/Sources/QuotaBar/QuotaModels.swift`
- `quota-bar/Sources/QuotaBar/InstallDetectorProvider.swift`
- `quota-bar/Sources/QuotaBar/Strategies.swift`
- `quota-bar/Sources/QuotaBar/RefreshCoordinator.swift`
- `REQUIREMENTS.md`
- `DESIGN.md`

# 对话与行动记录

- 用户要求通用页新增语言选项（中文 / 英文）。
- 用户要求菜单栏图标模式说明只显示当前选项对应描述，不同时解释合并和拆分。
- 用户要求未激活状态不能移除激活。
- 用户要求模型页 provider 行按名称、供应商、访问模式组织，并包含 API 访问模式标识。
- 用户反馈 Agy / Antigravity 获取不到。
- `QuotaPreferences` 新增 `language` 字段，`LanguagePreference` 提供 `中文` / `English`，并保持旧配置默认中文。
- 通用页新增「语言」section；图标模式说明改为随 `IconModePreference` 当前值切换。
- 激活页「移除激活」按钮在当前未激活状态禁用。
- 模型页 provider subtitle 改为「供应商 | App, CLI, Web, API」格式。
- Antigravity 安装探测补充 `cliCommands = ["agy", "antigravity"]`，`InstallDetectorProvider` 改为遍历多个 CLI 候选，避免只查 `antigravity` 导致 Agy CLI 未命中。

# 完成工作

- 通用页语言选项已添加。
- 菜单栏图标模式说明按当前模式单独展示。
- 未激活状态下移除激活按钮禁用。
- 模型页 provider 元信息结构已调整并包含 API 模式。
- Agy 安装探测补强。
- requirements / design 已同步。

# 更新的需求 ID

- 更新 `[0.3.0-PM-A-002]`
- 更新 `[0.3.0-PM-A-003]`
- 更新 `[0.3.0-PM-A-004]`

# 更新的 README 或 DESIGN 章节

- 更新 `DESIGN.md` 的「偏好设置窗口」章节。

# 验证方式

- `cd quota-bar && swift build`：通过。
- `cd quota-bar && swift test`：通过，20 个测试全部通过。
- `make app`：通过，产物为 `quota-bar/build/20260701-143345-preferences-main/QuotaBar.app`。

# 备注

- 语言选项当前先持久化偏好，实际全局文案切换需要后续接入本地化资源。
- API 访问模式先作为 provider 能力标签展示，不代表当前所有 API 路径都已接入真实额度获取。
