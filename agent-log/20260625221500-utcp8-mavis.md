# 2026-06-25 v0.6.0 arch + kimi 第一批落地

- **用户原始 prompt**："B（每个 provider 真实拿订阅到期日）" → "headless 访问订阅页面" → "每个模型拆 worktree" → "执行之前先写到 requirement 里" → "你先完成第一批（ARCH + Kimi）"
- **分支 / 版本**：feat/v0.6.0-arch 基于 e657332、feat/v0.6.0-kimi 基于 0e9b809
- **任务开始时间**：2026-06-25 22:01
- **任务结束时间**：2026-06-25 22:15
- **是否提交**：是（2 个 commit 合到 main：0e9b809 arch、03dd984 kimi）

## 已阅读上下文

- `quota-bar/Sources/QuotaBar/QuotaModels.swift:295-345` — `subscriptionExpiresAt` 字段定义和 init
- `quota-bar/Sources/QuotaBar/DashboardEndpoints.swift` — 协议 + KimiSubscriptionStatParser 错塞 bug
- `quota-bar/Sources/QuotaBar/BrowserCookieProvider.swift` — 调 parser 拼 ProviderSnapshot
- `quota-bar/Sources/QuotaBar/BrowserCookieReader.swift` — cookie 协议 + InMemoryCookieReader
- `quota-bar/Sources/QuotaBar/MenuView.swift:865-929` — PlanHeader UI
- `quota-bar/Tests/QuotaBarTests/Placeholder.swift` — 现有测试基建
- `quota-bar/Package.swift` — Swift Testing 框架，swift-tools-version 6.2
- `REQUIREMENTS.md` v0.6.0 phase（上一轮已建）

## 对话与行动记录

### 关键决策
1. **删 fallback**：v0.3.0-UI-A-010 的 `quotas.map(\.resetsAt).max()` 被实测验证为「完全乱写」，v0.6.0 改 nil fallback。找不到真实到期日 → UI hide。
2. **协议设计**：`SubscriptionDateHarvester` 协议只关心「给一个 pageURL 和 page source → 给我 Date?」，**不**关心怎么抓页面。这样：
   - Kimi 走 API（已有 `subscriptionBalance.expireTime`）→ `KimiSubscriptionStatParser.parseSubscriptionExpiresAt`
   - Codex/Claude/Cursor/MiniMax/Antigravity 走 headless → `*Harvester` 实现 + `WKWebViewHeadlessLoader`
3. **WKWebView @MainActor**：macOS 上 WKWebView 必须在 main thread 创建/交互，整个 loader 用 `@MainActor` 隔离。`evaluateJavaScript` 的 completion 不一定在 main，wrap 进 `Task { @MainActor in }`。
4. **不污染用户浏览器 cookie**：用 `WKWebsiteDataStore.nonPersistent()`，注入的 cookie 只活在临时 store。

### arch worktree 路径小插曲
- 第一次 `git worktree add ../quota-bar-arch` 后，目录里能 cd 进去改文件，但 `git worktree list` 看不到这个 worktree（关联在某个时点丢了）
- 解决：mv 目录到 /tmp 备份 → `git worktree prune` → 重建 worktree → cp 文件回去 → commit
- 教训：完成 worktree 后第一时间 verify `git worktree list` 能看到，避免「目录存在但 git 不知道」的鬼状态

### 测试策略
- 测 `firstDate` 多格式解析（ISO 8601 fractional / plain / 美式 long / 短 / 中文）= 7 个
- 测 `ProviderSnapshot` hide fallback（无参数时 nil、显式传入时优先、loading snapshot、empty quotas）= 4 个
- 测 `WKWebViewHeadlessLoader` cookie 入口失败路径（empty、permission、cookie store）= 3 个
- 真 WKWebView 加载**不**测（依赖 macOS window server，CI 跑不动）—— 实际运行时验
- Kimi 测 `parseSubscriptionExpiresAt` 提取 expireTime、Work resetsAt == nil、Code 5h/周 resetsAt 仍保留、边界 = 5 个

### 实际改动
**arch commit (0e9b809)**:
- `quota-bar/Sources/QuotaBar/QuotaModels.swift` — 删 max(resetsAt) fallback + 改文档
- `quota-bar/Sources/QuotaBar/DashboardEndpoints.swift` — 协议加 `parseSubscriptionExpiresAt`
- `quota-bar/Sources/QuotaBar/SubscriptionDateHarvester.swift` — 新建
- `quota-bar/Sources/QuotaBar/WKWebViewHeadlessLoader.swift` — 新建
- 3 个测试文件（15 个 test cases）

**kimi commit (03dd984)**:
- `quota-bar/Sources/QuotaBar/DashboardEndpoints.swift` — KimiSubscriptionStatParser 加 parseSubscriptionExpiresAt + Work resetsAt 改 nil
- `quota-bar/Sources/QuotaBar/BrowserCookieProvider.swift` — 主路径 + Kimi 路径调 parser
- `quota-bar/Tests/QuotaBarTests/KimiSubscriptionStatParserTests.swift` — 5 个新 test

## 完成工作

- [x] v0.6.0 phase 在 REQUIREMENTS.md 注册（上一轮已做）
- [x] arch worktree: 协议 + loader + hide fallback + 15 个测试
- [x] kimi worktree: 错塞修复 + 真实 expireTime 提取 + 5 个测试
- [x] 2 个 commit 合到 main
- [x] REQUIREMENTS.md 6 个 ARCH + 4 个 DATA-A 任务勾完成
- [x] 2 个 worktree 清理

## 更新的需求 ID

- [x] [0.6.0-ARCH-A-000/001/002/003/001-test/003-test] — 全部完成
- [x] [0.6.0-DATA-A-000/001/002/002-test] — 全部完成

## 更新的 README 或 DESIGN 章节

无（v0.6.0 实施未结束；UI 收尾 + 5 个 headless provider 待第二批）

## 验证方式

- `cd quota-bar-arch/quota-bar && swift build` — 零 warning
- `cd quota-bar-arch/quota-bar && swift test` — 15 tests passed, 3 suites passed
- `cd quota-bar-kimi/quota-bar && swift build` — 零 warning
- `cd quota-bar-kimi/quota-bar && swift test` — 20 tests passed, 4 suites passed
- main 顶部 3 个 commit：
  ```
  03dd984 feat(v0.6.0): kimi — subscriptionBalance.expireTime 作为真实订阅到期日
  0e9b809 feat(v0.6.0): arch — 订阅日期抓取基础架构
  e657332 docs(requirements): v0.6.0 新建 phase — 真实订阅到期日（headless 抓取订阅页）
  ```

## 备注

### 当前 main 状态
- v0.6.0 第一批完成：arch + kimi
- 行为：Kimi dropdown 价格旁灰色标签现在显示真实 subscriptionBalance.expireTime（视觉上跟之前一样，因为 Kimi 的 Work 月度重置日 = subscription 到期日，但语义修正）
- 其他 provider（Codex/Claude/Cursor/MiniMax/Antigravity）：价格旁日期**消失**（v0.3.0 fallback 已删，harvester 还没接）。这是预期，等第二批 5 个 headless provider 落地后逐一恢复。

### 风险
- 删 fallback 后到第二批 5 个 provider 落地之间的窗口期：所有非 Kimi 的 provider 价格旁日期不显示
- 如果用户在这期间实测，可能误以为「date 功能没了」—— 需要在 commit message / README 里强调是「中间状态」
- 实际第二批不会很慢（每个 provider 几小时）

### 后续 worktree
- v0.6.0-data-b-codex — Codex
- v0.6.0-data-c-claude — Claude
- v0.6.0-data-d-cursor — Cursor
- v0.6.0-data-e-minimax — MiniMax
- v0.6.0-data-f-antigravity — Antigravity
- v0.6.0-ui-a — UI 收尾（hide 时 layout 不跳动 + tooltip）

### 顺手观察
- 当前 `.worktrees/` 里有 2 个游离 worktree：
  - `feat/price-update-date` (cba5143) — v0.3.0-UI-A-010 的旧 worktree，未清理
  - `feat/v0.6.0-ui-a` (e657332) — 别人在跑（DESIGN.md 改动未 commit），base 比 main 旧 1 个 arch commit
- 这两个不动，等用户或 worktree owner 自己处理
