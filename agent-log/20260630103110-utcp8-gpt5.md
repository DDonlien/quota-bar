# 20260630103110 - sub/main：Quota Bar 显示数据存储位置分析

## 用户原始 prompt

> quota bar现在的显示的各类信息是保存在哪里的？app内还是数据文件夹，这类应用的最佳实践是什么

补充：

> 别管，继续

## 启动运行时的分支和版本

- 分支：`sub/main`
- 提交：`57694eff7fa09d299a2b59404d7b4ca1beea525d`
- 备注：按规范先执行 `git pull`，但远端拉取超过约 1 分钟未返回，已中断；用户明确要求跳过并继续，因此本次分析基于当前工作区。

## 任务开始时间

2026-06-30 10:20 (UTC+8)

## 任务结束时间

2026-06-30 10:31 (UTC+8)

## 任务结束时是否执行了提交

否。

## 已阅读上下文

- `AGENTS.md`
- `README.md`
- `REQUIREMENTS.md`
- `DESIGN.md`
- `agent-log/20260629014432-utcp8-mavis.md`
- `quota-bar/Sources/QuotaBar/QuotaModels.swift`
- `quota-bar/Sources/QuotaBar/AgentStore.swift`
- `quota-bar/Sources/QuotaBar/PreferencesStore.swift`
- `quota-bar/Sources/QuotaBar/RefreshCoordinator.swift`
- `quota-bar/Sources/QuotaBar/PlaceholderProviders.swift`
- `quota-bar/Sources/QuotaBar/ProviderFactory.swift`
- `quota-bar/Sources/QuotaBar/Strategies.swift`
- `quota-bar/Sources/QuotaBar/CodingPlanMenuApp.swift`
- `quota-bar/Sources/QuotaBar/StatusBarController.swift`
- `quota-bar/Sources/QuotaBar/DashboardEndpoints.swift`
- `quota-bar/Sources/QuotaBar/KeychainProvider.swift`

## 对话与行动记录

- 用户询问 Quota Bar 当前显示的各类信息保存在哪里，以及这类 app 的最佳实践。
- 按项目规范尝试 `git pull`，但命令长时间无返回；用户指示忽略后继续。
- 通过源码确认启动入口 `StatusBarController` 默认创建 `RefreshCoordinator(providers: ProviderFactory.createProviders(), installDetectors: ProviderFactory.createInstallDetectors())`。
- 确认 `ProviderFactory` 使用 `ProviderPipelines.makePipelines()`，运行时数据来自 Codex / Claude / MiniMax / Kimi / Antigravity 等 provider pipeline，而不是 `PlaceholderProviders.defaults()`。
- 确认 `PreferencesStore` 将用户偏好写入 `~/Library/Application Support/QuotaBar/preferences.json`。
- 确认 `KeychainProvider` 只读取凭证，不保存额度数据。

## 完成工作

- 明确当前显示数据分层：
  - 额度/订阅快照：刷新时 provider 实时获取，进入内存中的 `RefreshCoordinator.state` / `DashboardState`。
  - 用户偏好：写入 `~/Library/Application Support/QuotaBar/preferences.json`。
  - 凭证/登录态：读取各服务自身配置、浏览器 Cookie 或 macOS Keychain，不由 Quota Bar 自建凭证仓库。
  - 静态样例：`PlaceholderProviders.swift` 仍存在，但当前主启动路径未使用。
- 准备向用户说明最佳实践：偏好进 Application Support/UserDefaults，敏感凭证进 Keychain，缓存快照可短期保存到 Application Support/Caches，真实来源仍应优先实时刷新并标注时间。

## 更新的需求 ID

无。

## 更新的 README 或 DESIGN 章节

无。

## 验证方式

- 静态阅读源码与文档。
- 未运行构建或测试；本次无代码行为变更。

## 备注

- 本次只新增执行日志。
