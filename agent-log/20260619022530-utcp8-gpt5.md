# 任务执行日志：移除假订阅占位、修正订阅价格与刷新日期显示

## 用户原始 prompt

1. 成功了，但只有codex的信息是准的，而且钱也不准
2. kimi和gemini等待dashboard接入是什么鬼，另外，你都识别出我是什么档位的订阅了，怎么没有从网上直接获取对应的订阅价格呢？
3. MiniMax 为什么没有接上？另外如果没有接上的话，为什么能识别出我是什么档位的订阅呢？
- 根据问题 23，检查一下你现在的订阅档次。这些 Plus、Pro 是不是都乱写了，没有真正用获取的数据来返回？
4. 我电脑上根本没有 Claude，为什么会有 Claude？
5. 像 Codex 这种 6 天后的，你就直接显示日期吧，否则反应不过来啊；明天和后天你可以显示明天和后天

## 启动运行时状态

- 分支：`main`
- 起始版本：`926bd65`
- 开始时间：2026-06-19 02:14 CST 左右
- 结束时间：2026-06-19 02:25 CST
- 是否提交：否

## 已阅读上下文

- `AGENTS.md`
- `README.md`
- `REQUIREMENTS.md`
- `agent-log/20260619011202-utcp8-gpt5.md`
- 相关 Swift 源码：`QuotaModels.swift`、`BrowserCookieProvider.swift`、`KeychainProvider.swift`、`CLILogProvider.swift`、`DashboardEndpoints.swift`、`MenuView.swift`

## 行动记录

- 执行 `git pull`，结果为 `Already up to date`。
- 确认假订阅来源：
  - `ProviderKind.displayName` 把 Codex/MiniMax/Kimi 写死为 Plus。
  - `fallbackMonthlyPrice` 把 Codex/MiniMax/Kimi 写死为 `¥150/月`。
  - `BrowserCookieProvider` 在未接入 dashboard 时仍返回 available + 100% 占位额度。
  - `KeychainProvider` 在凭证存在时也返回 available + 占位额度，凭证缺失又会把 Claude/MiniMax 等显示为待配置。
- 将 provider 名称改为基础名，新增 `ProviderSnapshot.subscriptionTier`，只有真实响应返回 plan/tier 后才显示 Plus/Pro。
- 将 `fallbackMonthlyPrice` 改为可空，不再为未验证 provider 注入 `¥150/月`。
- `CodexAuthProvider` 和 `BrowserCookieProvider` 从 dashboard 响应里解析 `plan_type`，用 `ProviderPricing` 映射已验证的 OpenAI/Codex Plus/Pro 官方价格。
- 移除 Gemini/MiniMax/Kimi 的未验证价格映射；未接入 dashboard 或未知档位显示 `—`。
- `BrowserCookieProvider` 在 endpoint 未接入时抛 `sourceUnavailable("未接入 dashboard")`，不再显示“等待 dashboard 接入”的假额度。
- `KeychainProvider` 不再生成 available 占位；Keychain 只能作为凭证探测，不参与费用和额度展示。
- 新增 `QuotaResetText`，将较远刷新时间显示为 `M月d日`，明天/后天显示自然语言。
- 更新 `README.md` 和 `REQUIREMENTS.md`，记录“只有真实 usage/dashboard 响应才展示订阅和价格”的规则。

## 验证方式

- `cd drop-down-test/CodingPlanMenu && swift build`：通过。
- `cd drop-down-test/CodingPlanMenu && ./build-app.sh`：通过。
- 已关闭旧进程并打开新构建，进程 PID 为 `51635`。

## 需求更新

- 新增并完成 `SUB-A-009`：未拿到真实 dashboard/usage 响应时不生成 Plus/Pro、价格或 100% 额度占位。
- 新增并完成 `SUB-A-010`：订阅价格只在真实返回 plan/tier 后按官方价目映射。
- 新增并完成 `UI-C-006`：远期刷新时间显示具体日期，明天/后天使用自然语言。

## 设计更新

- 无视觉规范变更。

## 备注

- `.build/` 和 `.DS_Store` 仍是构建产物 dirty，未回滚。
- MiniMax/Kimi/Gemini/Claude 目前没有可用的真实 dashboard parser，因此不会在菜单里显示订阅占位。后续要显示它们，需要先接入对应真实 usage/plan API。
