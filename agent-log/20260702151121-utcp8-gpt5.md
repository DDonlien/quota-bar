# 用户原始 prompt

> 你这里链路写的有些混乱，我觉得应该分成3层：
> 1. provider获取
> 1.1. app bundle
> 1.2. 配置文件
> 1.3. cli
> 1.4. api
> 2. 额度获取
> 2.1. 配置文件
> 2.2. cli指令
> 2.3. api指令
> 2.4. rpc是什么？
> 2.x. 其他还有什么手段？通过 CLI 或者 API 直接发起自然语言问询，尝试让 AI 自己返回这个结果，有可能吗？浏览器在这里要怎么处理？
> 3. 过期日获取
> 3.1. 2.1. 配置文件
> 3.2. cli指令
> 3.3. api指令
> 3.x. 其他还有什么手段？通过 CLI 或者 API 直接发起自然语言问询，尝试让 AI 自己返回这个结果，有可能吗？浏览器在这里要怎么处理？
>
> 每次刷新时，执行逻辑应该是：
> 1. 先检查一遍所有的 provider；对于已有的 provider，应该用最近一次检查到它的手段进行复查，确保它还在；如果还在，跳过其余检查步骤；等所有已知的 provider 检查完了，按顺序把第一步跑一遍，检查新的，或者以已知手段没检查到的、老的 provider
> 2. 检查所有 provider 的额度，优先按照每个 provider 上次检查成功的额度检查手法检查。如果检查到了就完成；如果没检查到，按顺序全部都重新跑一遍额度获取手段
> 3. 检查过期日和额度一样，只不过具体检查的指令和位置可能不同，但逻辑也是一样的。先按上次成功的手法检查老的，检查不到就全部都跑一遍。
>
> 你现在应该先完善我列出的第一大段里所有的手法，然后结合第二大段，告诉我这个逻辑思路是否正确
>
> 然后你还应该列出3张表，每一张的纵轴分别是：
> 1. 第一步的可行方案
> 2. 第二步的可行方案
> 3. 第三步的可行方案
> 横轴是不同的 provider，然后单元格内容应该是针对这个 provider 的特殊处理或者检查方式。比如 Web 的话应该是对应的域名，CUI 的话应该是对应的指令

# 启动运行时的分支和版本

- 分支：`sub/main`
- 提交：`bc1ee47d8ad7a853618a539e0cf4ea9613fd7ec8`
- 已执行：`git pull`，结果为 already up to date。

# 任务开始时间

2026-07-02 14:56 Asia/Shanghai

# 任务结束时间

2026-07-02 15:11 Asia/Shanghai

# 任务结束时是否执行了提交

未提交。

# 已阅读上下文

- `REQUIREMENTS.md`
- `agent-log/20260702145131-utcp8-gpt5.md`
- `quota-bar/Sources/QuotaBar/DashboardEndpoints.swift`
- `quota-bar/Sources/QuotaBar/AgentDetector.swift`
- `quota-bar/Sources/QuotaBar/MiniMaxCLIProvider.swift`
- `quota-bar/Sources/QuotaBar/KimiAuthProvider.swift`
- `quota-bar/Sources/QuotaBar/CodexAuthProvider.swift`
- `quota-bar/Sources/QuotaBar/InstallDetectorProvider.swift`

# 对话与行动记录

- 用户指出上一版链路混乱，应拆成 Provider 获取、额度获取、过期日获取三层，并要求逐层列出手法、刷新逻辑判断和三张 provider 矩阵。
- 检查当前代码后确认：现有实现混用了 provider 发现、配置文件、CLI、API、browser fallback 的概念；MiniMax 的 `MiniMaxCLIProvider` 实际是读配置文件后调用 API，不是执行 CLI 指令。
- 更新 `REQUIREMENTS.md` 的 `ARCH-B`，从单一链路改为三层链路与三张能力矩阵。

# 完成工作

- 更新 `REQUIREMENTS.md`：
  - `0.9.0-ARCH-B-000` 至 `0.9.0-ARCH-B-008` 改为三层获取链路、来源索引、刷新逻辑、CLI/RPC、Browser、订阅状态归一化相关要求。
- 准备在回复中交付三层手法、刷新逻辑判断和三张 provider 矩阵。

# 更新的需求 ID

- `0.9.0-ARCH-B-000`
- `0.9.0-ARCH-B-001`
- `0.9.0-ARCH-B-002`
- `0.9.0-ARCH-B-003`
- `0.9.0-ARCH-B-004`
- `0.9.0-ARCH-B-005`
- `0.9.0-ARCH-B-006`
- `0.9.0-ARCH-B-007`
- `0.9.0-ARCH-B-008`

# 更新的 README 或 DESIGN 章节

无。

# 验证方式

- 文档变更，无构建验证。
- 使用 `rg` / `sed` 检查现有 provider 发现、endpoint、Codex/Kimi/MiniMax 等实现细节。

# 备注

- 未修改 Swift 实现代码。
- 工作区仍有未跟踪 agent-log 文件，未执行 git add/commit。
