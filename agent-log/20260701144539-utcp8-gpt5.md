# 用户原始 prompt

> 第二行这四个是“选项”你实际应该按照我们支持的方式“显示”

# 启动运行时的分支和版本

- 分支：`preferences/main`
- 提交：`358a2fb`
- 备注：延续偏好设置 UI 调整任务，当前工作区已有未提交改动；本轮未执行 `git pull`。

# 任务开始时间

- 2026-07-01 14:40:00 +0800

# 任务结束时间

- 2026-07-01 14:45:39 +0800

# 任务结束时是否执行了提交

- 否。

# 已阅读上下文

- `quota-bar/Sources/QuotaBar/Preferences/ModelsSettingsView.swift`
- `quota-bar/Sources/QuotaBar/Strategies.swift`
- `quota-bar/Sources/QuotaBar/MiniMaxCLIProvider.swift`
- `quota-bar/Sources/QuotaBar/MiniMaxConfigProvider.swift`
- `REQUIREMENTS.md`
- `DESIGN.md`

# 对话与行动记录

- 用户指出模型页第二行的 App / CLI / Web / API 是可选接入方式，应按当前真实支持情况展示，不能固定列出。
- 检查当前 pipeline：Codex 支持 CLI/Web；MiniMax 支持 CLI/Web/API；Kimi 支持 CLI/Web；Claude 支持 Web；GLM 尚未接入真实获取。
- 修改 `ModelsSettingsView.providerAccessModes`，按当前 pipeline 真实支持情况输出。
- GLM 改为显示 `API（待接入）`，避免误导成已接入。
- 同步更新 `REQUIREMENTS.md` 和 `DESIGN.md` 中模型页展示规则。

# 完成工作

- 模型页第二行现在按真实接入方式展示。
- 不再给 Codex / Kimi / Claude 硬塞 API 或 App。
- MiniMax 保留 CLI / Web / API，因为当前确有 CLI、Cookie Web 和 API key config provider。

# 更新的需求 ID

- 更新 `[0.3.0-PM-A-003]`

# 更新的 README 或 DESIGN 章节

- 更新 `DESIGN.md` 的「偏好设置窗口」章节。

# 验证方式

- `cd quota-bar && swift build`：通过。
- `cd quota-bar && swift test`：通过，20 个测试全部通过。
- `make app`：通过，产物为 `quota-bar/build/20260701-144516-preferences-main/QuotaBar.app`。

# 备注

- 当前展示语义为“现有 pipeline 支持/待接入”，不是 provider 理论能力清单。
