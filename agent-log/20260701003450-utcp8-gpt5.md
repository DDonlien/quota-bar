# 用户原始 prompt

> 移除这几个选项：
> - 模型：cursor、gemini、openai、deepseek、copilot、oupenrouter、perplexity、warp、trae、antigravity，把这些订阅的获取加入requirements，
> - 激活：说明、当前状态和本机的标题文字移除，保留未激活做成2行，第二行是一个文字输入，输入激活邮箱；
> - 关于：移除许可，开发者显示Taobe，移除维护的标题，移除平台
>
> 修改这几个选项：
> - 模型：加上GLM（智谱的模型）
> - 菜单栏：图标模式：改成合并、拆分就好
> - 激活：增加一个移除激活的按钮，设备id你显示的是啥？

补充：

> 侧边栏收起的按钮还在，记得移除

# 启动运行时的分支和版本

- 分支：`preferences/main`
- 提交：`358a2fb`
- 备注：延续偏好设置 UI 调整任务，当前工作区已有未提交改动；本轮未执行 `git pull`。

# 任务开始时间

- 2026-07-01 00:25:00 +0800

# 任务结束时间

- 2026-07-01 00:34:50 +0800

# 任务结束时是否执行了提交

- 否。

# 已阅读上下文

- `REQUIREMENTS.md`
- `DESIGN.md`
- `quota-bar/Sources/QuotaBar/QuotaModels.swift`
- `quota-bar/Sources/QuotaBar/PreferencesStore.swift`
- `quota-bar/Sources/QuotaBar/Preferences/ModelsSettingsView.swift`
- `quota-bar/Sources/QuotaBar/Preferences/GeneralSettingsView.swift`
- `quota-bar/Sources/QuotaBar/Preferences/ActivationSettingsView.swift`
- `quota-bar/Sources/QuotaBar/Preferences/AboutSettingsView.swift`
- `quota-bar/Sources/QuotaBar/Preferences/PreferencesScene.swift`
- `quota-bar/Sources/QuotaBar/StatusBarController.swift`

# 对话与行动记录

- 用户要求模型页移除多个 provider 开关，新增 GLM，并将被移除 provider 的订阅获取登记到 requirements。
- 用户要求激活页移除 section 标题和设备 ID 占位，保留未激活、激活邮箱输入，并增加移除激活按钮。
- 用户要求关于页精简字段：开发者显示 Taobe，移除许可、平台、维护标题。
- 用户要求菜单栏图标模式文案改成合并/拆分。
- 用户再次要求移除侧边栏收起按钮。
- 新增 `ProviderKind.glm` 基础元数据；模型设置页改为只展示 Codex / MiniMax / Kimi / Claude / GLM。
- `IconModePreference.displayName` 改为「合并」「拆分」，通用页说明同步改写。
- `QuotaPreferences` 新增 `activationEmail`，并实现向后兼容解码；`PreferencesStore` 新增 `setActivationEmail`。
- 重写激活页为未激活状态、邮箱输入、移除激活按钮；移除旧的设备 ID 占位。
- 关于页改为开发者 Taobe，移除许可、平台和维护标题。
- `PreferencesScene` 绑定 `NavigationSplitViewVisibility` 并固定为 `.all`，配合 `.toolbar(removing: .sidebarToggle)` 处理侧边栏收起入口。
- 在 `REQUIREMENTS.md` 登记 Cursor / OpenAI / DeepSeek / Copilot / OpenRouter / Perplexity / Warp / Trae / Antigravity / Gemini / GLM 的订阅获取后续任务。

# 完成工作

- 模型页显示范围和 GLM 已调整。
- 通用页图标模式文案已调整。
- 激活页和关于页结构已按用户要求精简。
- 设备 ID 占位已移除；此前显示的是基于 app bundle path hash 派生的伪 ID，并非真实硬件 ID。
- 侧边栏 visibility 改为固定显示，避免用户折叠 sidebar。
- requirements 和 DESIGN 已同步。

# 更新的需求 ID

- 更新 `[0.3.0-PM-A-003]`
- 更新 `[0.3.0-PM-A-004]`
- 更新 `[0.3.0-PM-A-005]`
- 新增 `[0.4.0-DATA-C-001]` 到 `[0.4.0-DATA-C-011]`

# 更新的 README 或 DESIGN 章节

- 更新 `DESIGN.md` 的「偏好设置窗口」章节。

# 验证方式

- `cd quota-bar && swift build`：通过。
- `cd quota-bar && swift test`：通过，20 个测试全部通过。
- `make app`：通过，产物为 `quota-bar/build/20260701-003340-preferences-main/QuotaBar.app`。

# 备注

- 本轮只从模型设置页隐藏未展示 provider，不从底层 enum 或数据 pipeline 移除它们，避免破坏历史偏好和后续订阅获取任务。
