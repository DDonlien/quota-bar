# 20260630-004605 - 将 macos build/latest 改为相对软链

## 用户原始 prompt

> 把它改成相对链接，也就是获取这个 latest 文件夹所在的同一层级的其他文件夹中最新的那个链接

## 启动运行时的分支和版本

- 分支：`main`
- 提交：`1532061`
- 任务开始状态：`main...origin/main [ahead 1]`

## 任务开始时间

2026-06-30 00:39:00 CST (+0800)

## 任务结束时间

2026-06-30 00:46:05 CST (+0800)

## 任务结束时是否执行了提交

否。

## 已阅读上下文

- 根目录 `AGENTS.md`
- 根目录 `README.md`
- 根目录 `REQUIREMENTS.md`
- 根目录 `DESIGN.md`
- 最新相关执行日志 `agent-log/20260629-214928-utcp8-taobe-gpt-5.md`
- `macos/scripts/build-app.sh`
- `.github/workflows/release.yml`
- `Makefile`

## 对话与行动记录

- 用户要求将 `macos/build/latest` 改为相对链接，并按同级目录中最新的时间戳构建目录建立链接。
- 按协作规范执行 `git fetch --prune`，远端追踪信息更新完成，当前分支未落后远端。
- 检查现有 `macos/build/latest`，确认当前链接目标为 `20260626-160957`，且 `QuotaBar.app` 构建产物被 Git ignore。
- 修改 `macos/scripts/build-app.sh`：新增 `BUILD_ROOT`，在更新 `latest` 时扫描 `build/` 同级时间戳目录，排序后取最新目录名，并用该目录名创建相对 symlink。
- 修改 `README.md`：说明 `build/latest/QuotaBar.app` 是相对软链，并指向 `build/` 同级时间戳目录中最新的一次构建。
- 修改 `REQUIREMENTS.md`：在 v0.5.0 工程卫生中新增并完成 `0.5.0-ENG-A-004`。
- 将当前 `macos/build/latest` 重新写为相对 symlink `20260626-160957`。

## 完成工作

- `macos/build/latest` 规则改为相对软链。
- `build-app.sh` 不再把绝对路径写入 `latest`。
- README 与需求记录已同步。

## 更新的需求 ID

- 新增并完成 `0.5.0-ENG-A-004`。

## 更新的 README 或 DESIGN 章节

- `README.md`：打包成 `.app` 章节中补充 `build/latest/QuotaBar.app` 的相对软链规则。
- `DESIGN.md`：无更新。

## 验证方式

- `bash -n macos/scripts/build-app.sh` 通过。
- `readlink macos/build/latest` 返回 `20260626-160957`。
- `test -d macos/build/latest/QuotaBar.app` 通过。
- `git diff --stat` 确认仅修改 README、REQUIREMENTS 与打包脚本。

## 备注

- 本次未执行 `make app`，避免额外生成新的 `.app` 构建产物；已用脚本语法检查与现有构建链接解析验证规则。
