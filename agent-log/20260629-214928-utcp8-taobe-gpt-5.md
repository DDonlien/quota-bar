# 20260629-214928 - 恢复 main 到根目录并保留单数 worktree

## 用户原始 prompt

> 合理，改回来，一并修改agent-template里的agents.md

## 启动运行时的分支和版本

- 分支：`main`
- 提交：`7d2f87d`
- 任务开始状态：上一轮已把 Git 元数据迁到 `.repo/.git`，`main` 工作区迁到 `worktree/main/`；用户确认这不利于 GitHub Desktop / Codex / 其他 Agent 发现 repo 与旁路 worktree。
- 额外未提交改动：`site/src/...` 与 logo 图片已有未提交改动，本轮不修改、不回滚。

## 任务开始时间

2026-06-29 21:42:00 CST (+0800)

## 任务结束时间

2026-06-29 21:49:28 CST (+0800)

## 任务结束时是否执行了提交

否。

## 已阅读上下文

- `agent-template/AGENTS.md` 的 Worktree 工作目录模式规则
- 根 `AGENTS.md`、`README.md`、`REQUIREMENTS.md`
- 当前 Git worktree 状态与 linked worktree `.git` 指针

## 对话与行动记录

- 用户确认更合理的布局是 `main` 放在项目根目录，其他分支继续放单数 `worktree/`。
- 将 `worktree/main/` 中的 `main` 工作区内容移动回项目根目录。
- 将 `.repo/.git` 还原为根目录 `.git`，删除空 `.repo/`。
- 删除 `.git/config` 中上一轮残留的 `core.worktree = ../../worktree/main`。
- 将所有 `worktree/*/.git` 指针从 `.repo/.git/worktrees/...` 修回根 `.git/worktrees/...`。
- 删除空的 `worktree/main/`。
- 修改 `AGENTS.md` 与 `agent-template/AGENTS.md`：`main` 默认保留在项目根目录，非 `main` worktree 使用单数 `worktree/`。
- 修改 `README.md` 的本地 worktree 布局说明。
- 修改 `REQUIREMENTS.md`：将上一轮严格 `.repo/` / `worktree/main/` 方案标记为已撤回，并新增完成项记录恢复根目录 `main`。

## 完成工作

- 项目根目录重新成为 `main` 工作区。
- `worktree/` 保持单数目录名，并只承载其他分支工作区。
- GitHub Desktop / Codex / 其他 Agent 可重新以项目根目录作为 repo 入口。
- `agent-template/AGENTS.md` 已同步为该折中规则。

## 更新的需求 ID

- `0.8.0-DOC-A-007`：标记 `#cut`，说明撤回原因。
- `0.8.0-DOC-A-008`：标记 `#cut`，说明撤回原因。
- `0.8.0-DOC-A-009`：标记 `#cut`，说明撤回原因。
- 新增并完成 `0.8.0-DOC-A-010`。
- 新增并完成 `0.8.0-DOC-A-011`。

## 更新的 README 或 DESIGN 章节

- `README.md`：本地 worktree 布局改为根目录 `main` + 单数 `worktree/`。
- `DESIGN.md`：无更新。

## 验证方式

- `git rev-parse --show-toplevel` 返回项目根目录。
- `git worktree list --porcelain` 第一项为项目根目录 `main`，其他分支在 `worktree/` 下。
- `worktree/*/.git` 指向根 `.git/worktrees/...`。
- 文档扫描确认活规则不再要求默认使用 `.repo/` 或 `worktree/main/`。

## 备注

- `site/src/components/Footer.astro`、`MenuBarMockup.astro`、`Nav.astro`、`site/src/layouts/Layout.astro` 与 logo 图片在本轮前已存在未提交改动；本轮只保留，不解释其业务内容。
