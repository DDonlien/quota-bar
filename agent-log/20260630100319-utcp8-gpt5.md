# 2026-06-30 10:03 — Preferences sidebar 恢复 macOS 原生组件

## 用户原始 prompt

> 还是不对，我说了，要用macos26的原生侧边栏组件

## 启动运行时的分支和版本

- 分支：`preferences/main`
- HEAD：`358a2fb` refactor(preferences): 背景色从 .regularMaterial 玻璃改极浅灰（对齐软件更新页）
- 上游：`origin/feat/preferences`，本地 ahead 6
- 本轮沿用之前用户授权：此前 `git pull` 因 GitHub 443 连接超时失败，用户要求“忽略，直接改”。

## 任务开始时间

- 2026-06-30 10:00 (Asia/Shanghai, UTC+8)

## 任务结束时间

- 2026-06-30 10:03 (Asia/Shanghai, UTC+8)

## 任务结束时是否执行了提交

- 否

## 已阅读上下文

- 用户提供的 sidebar 截图
- `PreferencesScene.swift`
- `DESIGN.md`
- `REQUIREMENTS.md`

## 对话与行动记录

- 用户明确指出：左侧必须使用 macOS 26 原生 sidebar 组件，而不是手搓卡片。
- 将 Preferences 根容器恢复为 `NavigationSplitView`。
- 左侧恢复为原生 `List(selection:)` + `.listStyle(.sidebar)` + `navigationSplitViewColumnWidth(...)`。
- 删除上一轮自绘 `sidebarCard` 的 `.ultraThinMaterial` 背景、手工圆角、阴影和描边。
- 保留右侧 detail 的自定义 `SettingsPage` / group / row 设计。
- 文档同步修正：以后左侧 sidebar 必须使用原生 `NavigationSplitView` + `List(.sidebar)`，即使某些系统 build 会显示 sidebar toggle，也优先保留原生 sidebar 视觉，不回退到自绘卡片。

## 完成工作

- [x] 左侧 sidebar 恢复 macOS 原生组件
- [x] 删除手工 sidebar card 背景
- [x] 新 app：`quota-bar/build/20260630-100256-preferences-main/QuotaBar.app`

## 更新的需求 ID

- `[0.3.0-PM-A-012]`

## 更新的 README 或 DESIGN 章节

- `DESIGN.md`「偏好设置窗口」

## 验证方式

- `cd quota-bar && swift build`
- `cd quota-bar && swift test`：20 tests in 4 suites passed
- `make app`：成功生成 `20260630-100256-preferences-main/QuotaBar.app`

## 备注

- 原生 sidebar 与“隐藏 sidebar toggle”在 macOS 26 不同 build 上可能存在系统行为差异。本轮按用户优先级选择原生 sidebar 视觉。
