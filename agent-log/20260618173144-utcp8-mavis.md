# 2026-06-18 17:31 — P0 + P1 + 公开发布 全部完成

## 用户原始 Prompt
- 完成 P0 + P1 + 公开给社区用，个人项目
- license=MIT（默认）/ cookie=SweetCookieKit（推荐）/ tcc=detect+UI引导（推荐）

## 启动时分支 / 版本
- 主仓库：/Users/taobe/Projects/GitHub/Personal/quota-bar
- HEAD：43fe088 update code
- 工作分支：feat/real-data-and-public（新建）
- worktree 路径：.worktrees/feat-real-data-and-public
- 开始时间：2026-06-18 16:50 (UTC+8)
- 结束时间：2026-06-18 17:31 (UTC+8)
- 已提交：4 commits（baseline + P0 + P1 + 公开准备）

## 完成工作

### 1. Worktree + 把 SUB-A 散落代码归位
- stash 抓取 main 工作区里 12 个 SUB-A 散落文件 → 新建 worktree → stash pop
- 发现 HEAD 的 StatusBarController 是 stub（无 coordinator），main 工作区有完整版但未提交
- 重写 StatusBarController 接好 RefreshCoordinator + Combine 订阅

### 2. P0：真实数据接入
- 引入 SweetCookieKit 0.4.0 SPM 依赖（Package.swift）
- FilesystemCookieReader 改用 BrowserCookieClient + Browser.defaultImportOrder
  - 错误归一化：privacyAccessDenied / cookieStoreUnavailable / loadFailed
- PrivacyAccessChecker：通过 ~/Library/Cookies/Cookies.binarycookies 探测 FDA，
  openFullDiskAccessSettings() 跳到系统设置
- MenuView 加橙色 PermissionBannerView（缺 FDA 时显示）
- CodingPlanMenuApp 注册 SweetCookieKit 的 keychain prompt handler
- KeychainProvider 改 kSecReturnData: true，新增 readToken() 实际读 token
- DashboardEndpoints 注册 Codex wham/usage + CodexDashboardParser 解析
- BrowserCookieProvider 重写：手动 Cookie: 头、401/403→missingCredentials
- ProviderFactory 扩展到 5 个 provider（Codex/Claude/Gemini/MiniMax/Kimi）
- 修复 MenuView 缺失常量 / StatusBarController 构造器

### 3. P1：可扩展性抽象
- ProviderFetchStrategy 协议 + FetchPipeline（parallel + sequential）
- Strategies.swift：QuotaProviderStrategy 适配 + ProviderPipelines.makePipelines()
- TTYCommandRunner：/usr/bin/script -q /dev/null 借 PTY，
  超时 SIGTERM→SIGKILL 升级
- LoginRunner 协议 + Codex/Claude/Gemini runner，调 AppleScript 跳 Terminal.app
- AgentProvider 折进 ProviderKind（同一枚举，5 个属性从 AgentModel 迁移）
- AgentDetector 改用 ProviderKind

### 4. 公开发布准备
- LICENSE（MIT，copyright Tao Be, 2026）
- README 重写：功能矩阵 / 支持 provider 表 / 安装构建 / 开发目录 /
  数据流 / TCC 引导 / PTY 与 login / 路线图 / 贡献 / 致谢
- CHANGELOG（Keep-a-Changelog 格式）
- .github/workflows/ci.yml：macos-14 + macos-15 matrix，SwiftPM cache，
  debug + release build
- .gitignore：覆盖 .build/ / .swiftpm/ / .DS_Store / Xcode / IDE / .worktrees/

### 5. 文档更新
- REQUIREMENTS.md：勾选 DA-A 全 5 项 + SUB-A 5/8 项（剩 Claude/Gemini endpoint 留 #deferred）
  + REF-A 全 4 项（剩 #deferred 状态栏图标动态化）+ UI-C 全 5 项 + X-B 全 7 项

## 验证方式
- swift build 通过（debug + release）
- swift run 启动后 4s 不崩溃
- 4 个 commits 干净 history，无误提交 .build/

## 已知缺口
- Claude / Gemini dashboard endpoint 未对接，仅做 Cookie 探测
- CLILogProvider 只支持 Codex
- CodexBar 的 Codex OAuth LoginFlow 没移植，目前用 LoginRunner 调 `codex login`
- 状态栏图标未按可用状态动态切换（保持固定 chart.bar.fill）
- AgentStore 仍未挂到 UI（detection 结果只内存里）

## 备注
- LICENSE 默认 MIT；用户想换跟我说一声
- 没动 main 工作区（除本次 stash 取了 SUB-A 文件）；主仓库另起 chore 清 .build/
- PR target：建议 merge 到 main → 推到 origin → tag v0.2.0 → GitHub Release 发包