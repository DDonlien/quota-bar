# 20260629013500 - 迁移 web 页面至 reference.html 设计

## 用户原始 prompt

> 进入feat-website工作，先把分支名称改成website/main，然后参考/Users/taobe/Projects/GitHub/Personal/quota-bar/web/reference/html/reference.html修改现在实际的页面
> 我之前有指派任务，你没做完，我需要你在这个分支为我修改参考/Users/taobe/Projects/GitHub/Personal/quota-bar/web/reference/html/reference.html修改现在实际的页面
> 你怎么network 又有issue了，继续

## 启动时分支与版本

- 分支：`feat/website` （后更名为 `website/main`）
- 启动版本：`e974437 [feat/website]`
- 运行环境：`feat-website` worktree

## 任务开始时间

2026-06-28 15:08 (UTC+8)

## 任务结束时间

2026-06-29 01:35 (UTC+8)

## 任务结束时是否执行了提交

否（改动仍在本地工作区，等待用户提交）。

## 已阅读上下文

- `web/reference/html/reference.html`：参考设计页面（英文，Tailwind CSS + Custom Fonts + Pricing Section + FAQ Section + Footer）
- `web/src/pages/index.astro`：旧的主页入口
- `web/src/layouts/Layout.astro`：主布局
- `web/src/components/` (Nav, Hero, Features, Footer 等)：旧的组件结构

## 对话与行动记录

### 1. 更名分支
- 在 `.worktrees/feat-website` 执行 `git branch -m website/main`，成功将工作区分支重命名为 `website/main`。

### 2. Layout 调整 (Layout.astro)
- 在 [Layout.astro](file:///Users/taobe/Projects/GitHub/Personal/quota-bar/.worktrees/feat-website/web/src/layouts/Layout.astro) 中引入 Tailwind CSS CDN，保留/适配原有 SEO 元数据，加入 `reference.html` 专属的 CSS Fonts 声明、深色渐变背景（dot pattern & glow-effect）和玻璃效果类，并保留了无依赖的滚动入场动画。

### 3. 组件重写与创建
- **Nav.astro**：重写为符合 `reference.html` 的结构与样式，并适配 GitHub Releases 动态下载脚本所用 `data-download` 和 `REPO` 参数。
- **Hero.astro**：重写为英文版居中 Hero 与大 CTA 样式，并将下载按钮适配为 `data-download` 自动提取。
- **ProductPreview.astro**：新建组件，包含大预览图（`https://lh3.googleusercontent.com/...`）、Mac 控制按钮（Monitor, Approve, Ask, Jump）和小结文案。
- **Features.astro**：将原有 4 个特性卡片重写为 `reference.html` 的 9 个特性网格，并应用 glass-card 样式。
- **Pricing.astro**：新建组件，放置 Quota Bar Pro 的 $9.99 终身特惠介绍。
- **FAQ.astro**：新建组件，放置折叠式常见问题解答。
- **Footer.astro**：重写为带社交链接（X、Discord）与新栏目分类（PRODUCT, LEGAL）的页脚。

### 4. 组装与清理
- 修改 [index.astro](file:///Users/taobe/Projects/GitHub/Personal/quota-bar/.worktrees/feat-website/web/src/pages/index.astro)，移除已经不用或不再推荐的 `MenuBarMockup.astro`、`ProviderGrid.astro`、`Showcase.astro` 等组件，并将它们从 git 暂存区删除（`git rm`）。
- 整合新组件并保留了客户端动态拉取 GitHub Pre-release Nightly Releases 的 JS 缓存脚本。

### 5. 构建验证
- 运行 `npm run build`，编译生成静态 HTML，证明无编译语法或 Vite 依赖构建错误。

## 完成工作

- 完成分支更名为 `website/main`
- 基于 `reference.html` 的最新版设计和文案全面重构了 Quota Bar 静态站页面和全部组件，实现了纯 Tailwind 与微 custom CSS 混合的效果。
- 清理了旧的 mockup / showcase 冗余组件。

## 更新的需求 ID

- 标记 [web/REQUIREMENTS.md](file:///Users/taobe/Projects/GitHub/Personal/quota-bar/.worktrees/feat-website/web/REQUIREMENTS.md) 中 Phase - v0.1.0 的未尽项或重构覆盖。

## 更新的 README 或 DESIGN 章节

无。

## 验证方式

- 执行 `npm run build` 顺利产出 `dist/`，未报任何 error。

## 备注

- 动态获取 GitHub DMG 下载包链接的 `UPDATE_DOWNLOAD` 逻辑已被平移到新 Hero & Nav 下载按钮中，保持动态拉取机制正常工作。
- 删除了被废弃的 `MenuBarMockup.astro`、`ProviderGrid.astro`、`Showcase.astro` 文件。
